{
  "type": "fix",
  "en": "Return the required `loaded` flag from the production starter's SessionExecutor.read, and ship a shared read-result contract so the example and host executors are held to the same interface as the SDK's own.",
  "zh-CN": "production starter 的 SessionExecutor.read 补齐必需的 loaded 字段，并新增读取结果契约检查，让示例与宿主实现和 SDK 自带实现受同一接口约束。"
}
