# Production Agent SDK Monorepo Roadmap

## Goal

Rebuild Blade Agent SDK into a production-grade monorepo inspired by
[`earendil-works/pi`](https://github.com/earendil-works/pi), with three core packages:

- `@blade-ai/ai`: provider-agnostic model API, streaming, usage, pricing, provider options.
- `@blade-ai/agent`: runtime-independent agent kernel, loop contracts, tool calling protocol, state, traces, permissions.
- `@blade-ai/agent-sdk`: session-first product SDK that composes `ai`, `agent`, and local/server adapters.

The public experience remains session-first:

```ts
import { createSession } from '@blade-ai/agent-sdk';
```

Backward compatibility is not a constraint. Architecture correctness, production quality, clear types, and agent-friendly APIs take priority.

Pi reference mapping:

- Pi `@earendil-works/pi-ai` maps to Blade `@blade-ai/ai`.
- Pi `@earendil-works/pi-agent-core` maps to Blade `@blade-ai/agent`.
- Pi `@earendil-works/pi-coding-agent` maps to Blade `@blade-ai/agent-sdk` adapters, but Blade keeps `createSession()` as the primary product API instead of making a CLI-first package the center.
- Pi `@earendil-works/pi-tui` remains an optional future adapter direction, not part of the core SDK split.

## Principles

1. Keep `createSession()` as the primary user-facing API.
2. Make package boundaries enforceable by tests, not convention.
3. Keep `@blade-ai/agent` free of Node-local dependencies, provider implementations, MCP SDK, filesystem, shell, sandbox, and storage implementations.
4. Keep `@blade-ai/ai` focused on model/provider concerns only.
5. Keep local tools, MCP, filesystem, shell, sandbox, and durable stores out of the agent kernel.
6. Develop every feature and bugfix with TDD: failing test, implementation, passing test, docs, commit.
7. Every phase must improve the verification chain before relying on it.
8. Release automation must publish from `main` after CI gates pass.

## Target Architecture

```text
packages/
  ai/
    src/
      index.ts
      model-port.ts
      providers/
      usage/
      pricing/
  agent/
    src/
      index.ts
      kernel/
      protocol/
      ports/
      state/
      tracing/
  agent-sdk/
    src/
      index.ts
      session/
      server/
      local/
      browser/
      tools/
```

Dependency direction:

```text
@blade-ai/agent-sdk -> @blade-ai/agent -> @blade-ai/ai
@blade-ai/agent-sdk -> local/server adapters
@blade-ai/agent     -> no Node-local runtime dependencies
@blade-ai/ai        -> provider SDKs only behind adapter boundaries
```

## Quality Gates

The final production verification chain must include:

- Static gates: `pnpm run lint`, `pnpm run type-check`, package boundary checks.
- Unit tests: provider adapters, agent kernel, session runtime, tool contracts.
- Integration tests: session stream, tool execution, hooks, traces, usage aggregation, compaction.
- Browser-safety tests: `core`/browser-safe entries cannot import Node-only modules.
- Package tests: `pnpm pack`, install tarballs into a temporary consumer, import each package and subpath.
- Build tests: every package emits JS and `.d.ts` with correct exports.
- Release dry-run: semantic-release validates package metadata and provenance.
- Optional live tests: GLM `glm-5.2` via `.env` `baseUrl` and `apiKey`, gated behind an explicit script.

Current guardrails:

- `pnpm run verify:boundaries` scans package source imports and fails if `@blade-ai/ai` depends on agent/session SDK layers, or if `@blade-ai/agent` imports Node-local runtime modules, MCP SDKs, or `@blade-ai/agent-sdk`.
- The same boundary verifier also checks package manifests so `@blade-ai/agent` cannot declare MCP SDKs, provider runtime SDKs, or local filesystem/terminal/storage dependencies.
- Package source relative imports and exports must include explicit runtime file extensions, keeping the emitted ESM package graph aligned with Node and bundler resolution.
- Package manifests for `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` must not expose CLI product capabilities through `bin`, `./cli`, or `cli` keywords; future Pi-style coding-agent / CLI product behavior belongs in a separate package.
- `pnpm run verify:release` keeps the workspace root as a private non-publishable orchestrator, rejects root `publishConfig` / `files` metadata, and fails any semantic-release npm plugin configuration that would publish the root package instead of the three package roots.
- `pnpm run test:live:glm` builds `@blade-ai/ai` and verifies one non-streaming plus one streaming request against a GLM/OpenAI-compatible endpoint using `.env` credentials.
- `pnpm run test:live:session-glm` builds all three packages and verifies a real session-first `createSession()` + `send()` + `stream()` turn against the same GLM/OpenAI-compatible endpoint with `allowedTools: []`.

## Roadmap

### Phase 0: Roadmap and Safety Baseline

Objective: Establish the execution contract and prevent accidental regression while the migration begins.

Deliverables:

- This roadmap.
- Baseline tests for package topology expectations.
- Baseline CI/release audit documenting current gaps.

Verification:

- `pnpm run build`
- `pnpm run verify:entrypoints`
- focused package topology tests

Commit:

- `docs: add production monorepo roadmap`

### Phase 1: Workspace Skeleton

Objective: Convert the repo from a single package into a pnpm monorepo without changing runtime behavior.

Deliverables:

- `pnpm-workspace.yaml` with `packages/*`.
- `packages/ai`, `packages/agent`, `packages/agent-sdk`.
- Root scripts that delegate through `pnpm -r`.
- Package-level `package.json`, `tsconfig.json`, `tsup.config.ts`.
- `@blade-ai/agent-sdk` remains the published session-first package.

TDD:

- Add a topology test that fails until all three package manifests exist.
- Add a package export test that fails until each package exposes an `index`.

Verification:

- `pnpm install --lockfile-only`
- `pnpm -r run build`
- `pnpm -r run type-check`
- `pnpm run test`

Commit:

- `build: introduce ai agent agent-sdk workspace skeleton`

Status:

- Implemented workspace skeleton with `packages/ai`, `packages/agent`, and `packages/agent-sdk`.
- Root package is now a private workspace orchestrator.
- `@blade-ai/agent-sdk` is the publishable session-first package and retains the current root, browser, server, session, tools, and local entrypoints during migration.
- Package builds use package-local `tsup.config.ts` plus `tsconfig.build.json` so JS and declaration output are verified independently.
- The publishable `@blade-ai/agent-sdk` build now derives external dependencies from `packages/agent-sdk/package.json`, not the private root orchestrator manifest, keeping package output tied to the package's own publish contract.
- The publishable `@blade-ai/agent-sdk` JS bundle now starts from package-local `packages/agent-sdk/src/*` public entry wrappers instead of direct `../../src/*` tsup entries, creating the first enforceable source-ownership seam before the remaining implementation and declaration output are migrated into the package.
- The browser-safe `@blade-ai/agent-sdk/browser` and `@blade-ai/agent-sdk/core` public source entries now live as explicit package-local contracts instead of wildcard-forwarding to root `src/browser` and `src/core`, while their deeper protocol type modules remain a later package-local migration step.
- The `@blade-ai/agent-sdk/core` JSON, model config, constants, and permission contract types now resolve through package-local `packages/agent-sdk/src/types/*` modules instead of root `src/types/*`, while deeper tool/runtime/session contracts remain staged for later ownership migration.
- The `@blade-ai/agent-sdk/core` tool kind and tool behavior contract now resolve through package-local `packages/agent-sdk/src/tools/types/ToolKind.ts`, while richer tool definition/result/effect contracts remain staged for later package-local migration.
- The `@blade-ai/agent-sdk/core` observability contract types now resolve through package-local `packages/agent-sdk/src/observability/types.ts`; runtime trace recording implementation remains outside browser-safe core.
- The `@blade-ai/agent-sdk/core` runtime contract types now resolve through package-local `packages/agent-sdk/src/runtime/types.ts`; runtime context merging and patch summarization helpers remain outside browser-safe core.
- The `@blade-ai/agent-sdk/core` session stream contract types now resolve through package-local `packages/agent-sdk/src/session/types.ts`; full `SessionOptions` and runtime session management remain outside browser-safe core.
- The `@blade-ai/agent-sdk/core` rich tool contract types now resolve through package-local `packages/agent-sdk/src/tools/types/index.ts`; tool runtime helpers, registries, catalogs, and execution implementations remain outside browser-safe core.

### Phase 2: Extract `@blade-ai/ai`

Objective: Move model/provider responsibilities behind a clean AI package.

Move from current SDK:

- `src/services/ChatServiceInterface.ts`
- `src/services/VercelAIChatService.ts`
- `src/services/RetryPolicy.ts`
- `src/services/deepseek.ts`
- model usage and provider option types that belong to model execution

Deliverables:

- `ModelPort`
- `ModelRequest`
- `ModelStreamEvent`
- `ModelResponse`
- `UsageInfo`
- provider adapters for OpenAI-compatible, OpenAI, Anthropic, Gemini, DeepSeek
- DeepSeek cost helpers in `@blade-ai/ai`

TDD:

- Provider config normalization tests.
- Stream event conversion tests.
- Usage aggregation tests.
- DeepSeek pricing/caching tests.
- No agent/session import allowed from `@blade-ai/ai`.

Live verification:

- Add `pnpm run test:live:glm` that reads `.env`, uses `glm-5.2`, and performs:
  - one non-streaming response
  - one streaming response
  - usage assertion when provider returns usage

Commit:

- `refactor(ai): extract provider runtime package`

Status:

- First extraction increment complete: retry policy now lives in `@blade-ai/ai/retry`.
- The legacy root path `src/services/RetryPolicy.ts` re-exports the package implementation so existing runtime code keeps working during migration.
- `@blade-ai/ai` now publishes a `./retry` subpath with JS and declaration output.
- Chat protocol types now live in `@blade-ai/ai/chat`, including `ChatConfig`, `Message`, `UsageInfo`, `StreamChunk`, `ChatResponse`, and `IChatService`.
- The legacy root `src/services/ChatServiceInterface.ts` now re-exports chat protocol types and keeps only the SDK-local `createChatServiceAsync()` factory.
- Model execution protocol types now live in `@blade-ai/ai/model`, including `ModelPort`, `ModelRequest`, `ModelStreamEvent`, `ModelResponse`, `ModelToolCall`, and model-scoped `UsageInfo`.
- The `@blade-ai/ai` root exports `Model*` protocol types and `ModelUsageInfo` while preserving the existing chat `UsageInfo` root export until the chat/runtime migration is complete.
- The first provider runtime adapter now lives in `@blade-ai/ai/providers/openai-compatible`, exposing a `ModelPort` over Vercel AI SDK's OpenAI-compatible provider and normalizing text, reasoning, tool calls, usage, and stream events.
- The GLM live test now supports both JSON `.env` files with `key/url` fields and conventional `GLM_API_KEY`/`GLM_BASE_URL` environment variables, normalizing gateway root URLs to `/v1`.
- The session SDK's `VercelAIChatService` now delegates `provider: "openai-compatible"` chat and stream execution through `@blade-ai/ai/providers/openai-compatible`, beginning the recomposition of SDK runtime code on top of `@blade-ai/ai`.
- Vercel AI provider construction now lives in `@blade-ai/ai/providers/vercel`, covering native OpenAI, Anthropic, Gemini, Azure OpenAI, DeepSeek, and custom OpenAI-compatible model factories; the session SDK consumes this factory instead of directly constructing provider SDK models.
- DeepSeek pure provider helpers now live in `@blade-ai/ai/deepseek`, including model normalization, endpoint selection, cache-aware usage and pricing helpers, cache-prefix optimization, long-context chunk planning, strict tool schema sanitization, and default DeepSeek model config.
- DeepSeek runtime fetch APIs now live in `@blade-ai/ai/deepseek`, including FIM completion, chat completion, batch chat completion, per-response usage parsing, cache optimization, and cost calculation; the legacy SDK `src/services/deepseek.ts` path re-exports these runtime functions during migration.
- A package boundary verifier now enforces key Pi-style dependency direction checks during the migration, including rejecting provider SDK dependencies from the session SDK package manifest.
- Generic Vercel AI execution now lives in `@blade-ai/ai/providers/vercel` as `createVercelModelPort`, covering generate and stream execution for native OpenAI, Anthropic, Gemini, Azure OpenAI, DeepSeek, and compatible fallback providers.
- `ModelRequest` now carries structured output format, assistant tool-call context, tool results, strict tool metadata, and provider options so model adapters own provider-runtime message/tool/output conversion.
- The OpenAI-compatible and generic Vercel ModelPort adapters now normalize structured output, assistant tool calls, tool results, usage, reasoning, stream events, and DeepSeek strict-tool/thinking options behind `@blade-ai/ai`.
- The session SDK's `VercelAIChatService` now delegates all provider chat and stream execution through `ModelPort`, with no direct `generateText`, `streamText`, `jsonSchema`, or `Output` runtime dependency in the session service.

### Phase 3: Extract `@blade-ai/agent`

Objective: Create a runtime-independent agent kernel with clear ports and protocol types.

Move or define:

- agent stream protocol
- tool call protocol
- turn state
- usage aggregation
- trace events
- permission decision contracts
- `AgentKernel`
- ports:
  - `ModelPort`
  - `ToolPort`
  - `PermissionPort`
  - `StorePort`
  - `HookPort`
  - `TracePort`

Must not include:

- `node:*`
- MCP SDK
- builtin local tools
- provider SDK implementations
- filesystem storage implementations
- shell/sandbox

TDD:

- Agent kernel no-tool turn test.
- Agent kernel tool-call turn test.
- Permission deny/allow tests.
- Abort handling test.
- Trace event emission test.
- Package boundary test forbidding Node-local imports.

Commit:

- `refactor(agent): extract runtime-independent agent kernel`

Status:

- First kernel increment complete: `@blade-ai/agent` now exposes a runtime-independent `AgentKernel.runTurn()` that executes a no-tool user turn through `@blade-ai/ai`'s `ModelPort` and emits content, usage, thinking, and result events.
- The first Phase 3 TDD guardrail lives in `packages/agent/src/__tests__/AgentKernel.test.ts`, proving the kernel can run without Node-local, MCP, provider SDK, filesystem, shell, sandbox, or session SDK dependencies.
- The second kernel increment adds the first tool-call turn loop: `AgentKernel` can execute `ModelResponse.toolCalls` through an injected `AgentToolPort`, emit `tool_use` and `tool_result` events, append assistant/tool messages, and perform a follow-up model call for the final answer.
- The third kernel increment adds `AgentPermissionPort` with allow/deny decisions before tool execution; denied tool calls do not reach `AgentToolPort`, emit an error tool result, and are fed back into the follow-up model call.
- The fourth kernel increment adds a minimal runtime-independent `AgentTracePort`, with TDD coverage proving that turn start, model request/response, tool call start/end, usage, and turn end activity can be recorded as structured trace events without changing the session-first stream API.
- The fifth kernel increment adds the first abort guard: if a turn starts with an already-aborted `AbortSignal`, `AgentKernel` emits a controlled `ABORTED` error event and does not call the model port.
- The sixth kernel increment adds multi-iteration tool loops with a `maxSteps` model-step limit, so the kernel can continue across repeated model tool-call responses while stopping runaway loops with a controlled `MAX_STEPS_EXCEEDED` error event.
- The seventh kernel increment adds a runtime-independent `AgentStorePort`, allowing the kernel to append newly created input, assistant, and tool messages with turn/step context while leaving filesystem, database, and session storage implementations to outer adapters.
- The eighth kernel increment adds a runtime-independent `AgentHookPort` for model lifecycle hooks: `beforeModel` can rewrite model requests, and `afterModel` can observe responses with turn, step, and message context, preparing the session hook runtime migration without coupling the kernel to SDK-local hooks.
- The ninth kernel increment splits `@blade-ai/agent` source ownership into package-local `kernel`, `protocol`, `ports`, `state`, and `tracing` modules while keeping the root package export stable. `AgentKernel` now lives in `src/kernel/AgentKernel.ts`, and protocol/port/state/trace contracts are independently importable within the package for future kernel growth.
- The tenth kernel increment publishes the kernel package modules as explicit subpath exports: `@blade-ai/agent/kernel`, `@blade-ai/agent/protocol`, `@blade-ai/agent/ports`, `@blade-ai/agent/state`, and `@blade-ai/agent/tracing`. Package verification now requires those JS/declaration files in the tarball and imports each subpath from a temporary consumer install.
- The eleventh kernel increment adds optional model-level streaming mode: `AgentKernel` can call `ModelPort.stream()`, emit incremental thinking/content/usage events, aggregate the streamed response, and continue through the same tool loop, trace, hook, and store path without depending on provider or runtime adapters.

### Phase 4: Rebuild `@blade-ai/agent-sdk`

Objective: Recompose the session-first SDK on top of `@blade-ai/ai` and `@blade-ai/agent`.

Deliverables:

- `createSession()`
- `resumeSession()`
- `forkSession()`
- `prompt()`
- server/local adapters:
  - local tool registry
  - MCP registry
  - filesystem/session store
  - sandbox
  - hooks runtime
- browser-safe entry and server-only stubs remain.

TDD:

- Existing session tests migrated to `packages/agent-sdk`.
- `allowedTools: []` disables all tools.
- sampling options flow into model config.
- tokenBudget flows into runtime.
- observability trace remains intact.

Commit:

- `refactor(agent-sdk): recompose session-first sdk on core packages`

Status:

- First adapter increment complete: `SessionKernelAdapter` bridges the existing session tool registry and execution pipeline into `AgentToolPort`, and `SessionRuntime.getKernelToolPort()` exposes registered local/MCP/custom tools to `AgentKernel` without coupling `@blade-ai/agent` to SDK-local session internals.
- Second adapter increment complete: `SessionKernelStoreAdapter` bridges `AgentStorePort` into the existing session `ContextManager`, preserving kernel-appended user, assistant, and tool messages in session-first history with kernel turn/step metadata.
- Third adapter increment complete: `SessionKernelTraceAdapter` bridges `AgentTracePort` into the existing session `TraceRecorder`, preserving kernel turn/model/tool/usage events under the session observability redaction and payload-capture policy.
- Fourth adapter increment complete: `SessionRuntime.createAgentKernel()` composes an `AgentKernel` from an injected `ModelPort` plus the session store, trace, and optional tool adapters, creating the first runtime-level seam for replacing the legacy session loop with the runtime-independent kernel.
- Fifth adapter increment complete: `AgentKernel` now accepts runtime-independent model request defaults, and `SessionRuntime.createAgentKernel()` can build its own `ModelPort` plus request defaults from the session `BladeConfig`, preserving provider, model, sampling, context, provider options, and thinking capability while keeping the kernel free of provider implementations.
- Sixth adapter increment complete: `SessionRuntime.streamAgentKernelTurn()` added a guarded kernel stream path that maps kernel content, thinking, tool, usage, error, and result events into the existing session `StreamMessage` protocol, creating the path that later became the public default.
- Seventh adapter increment complete: `SessionKernelHookAdapter` bridges the kernel `AgentHookPort` into the session `HookRuntime` for first-step prompt submission rewrites, so guarded kernel turns preserve session-first prompt hooks before `Session.stream()` is moved onto the kernel loop.
- Eighth adapter increment complete: `Session.stream({ experimentalKernel: true })` exposed the first session-first public switch onto the kernel stream path, preserving pending-message consumption, abort signal composition, trace finishing, task-completed hooks, tool execution context, and `session.messages` synchronization before the kernel runtime became the default.
- Ninth adapter increment complete: the kernel tool port now lists real model tool definitions instead of placeholder call shapes, and `AgentKernel` includes registered session tool schemas in model requests so the experimental session-first kernel path can advertise local/MCP/custom tools before executing tool calls.
- Tenth adapter increment complete: the kernel stream path now round-trips tool calls through session tools, feeds tool results back into the follow-up model request, emits session `tool_use`/`tool_result` events, and preserves assistant `tool_calls` plus tool `name`/`tool_call_id` when synchronizing `session.messages`.
- Eleventh adapter increment complete: the kernel stream path now preserves the pending user message when a turn is aborted before model execution, emits a controlled `ABORTED` stream error without calling the model, and finishes observability traces with `aborted` status.
- Twelfth adapter increment complete: kernel observability now records usage with the same session max context token limit used by stream `usage` events, keeping trace-based debugging and accounting aligned with the session-first public API.
- Thirteenth adapter increment complete: kernel tool results now preserve permission update effects, emit session `tool_permission_updates` events before `tool_result`, and record the same updates into the session trace so permission state remains observable through the kernel path.
- Fourteenth adapter increment complete: `Session.stream()` now uses the runtime-independent kernel path by default, while `stream({ runtime: 'legacy' })` remains as an explicit migration escape hatch for old loop behavior; the deprecated `experimentalKernel` flag is still accepted during the transition.
- Fifteenth adapter increment complete: the `@blade-ai/agent-sdk` package build now resolves every public JS entry (`.`, `browser`, `server`, `session`, `tools`, `local`, and `core`) through package-local source wrappers, with a topology test preventing the package tsup config from pointing directly at root `../../src/*` entry files. This is an incremental package-ownership step; the wrapped implementations and declaration emit still need to move package-local in later slices.
- Sixteenth adapter increment complete: the browser-safe `browser/index`, `browser/server-only-stub`, and `core/index` public source entries are now owned inside `packages/agent-sdk/src`, with a topology guard preventing them from regressing to root wildcard forwarders. These entries still reference deeper root protocol/type modules until those modules are migrated package-local in later slices.
- Seventeenth adapter increment complete: `@blade-ai/agent-sdk/core` now owns its JSON value/object types, model and sandbox config contracts, constants, and permission request/result/update type contracts in package-local `src/types/*` files. Runtime permission handler factories remain in the root SDK implementation for now because they depend on non-contract validation/runtime code.
- Eighteenth adapter increment complete: `@blade-ai/agent-sdk/core` now owns `ToolKind` and `ToolBehavior` in package-local `src/tools/types/ToolKind.ts`, and the package-local permission contracts use that local tool kind type. `ToolDefinition`, `ToolConfig`, `ToolResult`, and `ToolEffect` still come from root tool contracts until their dependent runtime/session types are migrated.
- Nineteenth adapter increment complete: `@blade-ai/agent-sdk/core` now owns `AgentTrace`, `TraceEvent`, `TraceSpan`, `TraceSink`, and `ObservabilityOptions` as package-local observability contracts. `TraceRecorder` stays in the server/session implementation because it is runtime behavior, not a browser-safe contract.
- Twentieth adapter increment complete: `@blade-ai/agent-sdk/core` now owns `RuntimeContext`, `ContextSnapshot`, `RuntimePatch`, `RuntimeContextPatch`, and related runtime patch contract types in package-local `src/runtime/types.ts`. Root runtime helper functions such as `mergeContext`, `createContextSnapshot`, and `summarizeRuntimePatchApplications` stay in the implementation layer.
- Twenty-first adapter increment complete: `@blade-ai/agent-sdk/core` now owns `SendOptions`, `StreamOptions`, `StreamMessage`, and the message payload contracts needed by stream events in package-local `src/session/types.ts`. Broader session construction, MCP, persistence, hooks, and adapter runtime options stay in the session implementation layer.
- Twenty-second adapter increment complete: `@blade-ai/agent-sdk/core` now owns `ExecutionContext`, `ToolDefinition`, `ToolConfig`, `ToolResult`, `ToolEffect`, and related tool authoring contracts in package-local `src/tools/types/index.ts`. Root-side tool helper functions, validation adapters, registries, catalogs, background managers, and concrete tool execution remain implementation-layer concerns.
- Twenty-third adapter increment complete: `@blade-ai/agent-sdk/session` now has a package-local public source entry and package-local declaration overlay that export local session-first contracts (`SessionOptions`, `ISession`, resume/fork/prompt options, user message content, MCP/session status, token budget, hooks, and logger shapes) while wrapping the existing root session runtime. The package build now emits `dist/session/index.d.ts` from `packages/agent-sdk/src/session/index.ts` instead of the legacy root `src/session/index.ts`.
- Twenty-fourth adapter increment complete: `@blade-ai/agent-sdk/tools` now has a package-local public source entry and package-local declaration overlay that export local tool authoring contracts (`Tool`, `ToolConfig`, `ToolDefinition`, `ToolCatalog*`, `ToolKind`, `ToolErrorType`, and helper signatures) while wrapping the existing root tool factory and catalog runtime. The package build now emits `dist/tools/index.d.ts` from `packages/agent-sdk/src/tools/public-index.ts` instead of the legacy root `src/tools/index.ts`.
- Twenty-fifth adapter increment complete: the Node adapter public entries `@blade-ai/agent-sdk/server` and `@blade-ai/agent-sdk/local` now have package-local source ownership instead of wildcard-forwarding to root `src/server` / `src/local`. `server` is a package-local session-first facade over the package root, while `local` explicitly exposes MCP, memory, sandbox, builtin-tool, and memory-tool adapter modules from the implementation layer.
- Twenty-sixth adapter increment complete: the root `@blade-ai/agent-sdk` public entry now has package-local session-first source ownership instead of wildcard-forwarding to legacy root `src/index.ts`. Its public declaration overlay is emitted from package-local facade declarations, points only at final public subpath entries (`core`, `local`, `session`, and `tools`), and keeps `createSession()` as the root API while preserving compatibility exports for DeepSeek helpers, permission handlers, local adapters, subagents, and tool authoring contracts.
- Twenty-seventh adapter increment complete: `@blade-ai/agent-sdk/core` now owns the runtime permission handler factories (`createPermissionHandlerFromCanUseTool`, mode/rule/path-safety/composite handlers) in package-local `src/types/permissions.ts`, so root and core entries no longer import those factories from legacy root `src/types/permissions.ts`. The package-local path-safety handler keeps lightweight dangerous-path and sensitive-credential checks without pulling the legacy `SensitiveFileDetector` runtime into the core entry.
- Twenty-eighth adapter increment complete: `@blade-ai/agent-sdk/tools` now owns the public tool authoring runtime (`createTool`, `defineTool`, `toolFromDefinition`, `ToolCatalog`, invocation validation, zod-to-json-schema conversion, catalog source metadata, MCP removal, and simple search) inside package-local `src/tools/index.ts`. The tools entry no longer imports legacy root `src/tools/core/createTool.ts` or `src/tools/catalog/ToolCatalog.ts`; heavier builtin/local tool execution remains in the local adapter layer for later slices.
- Twenty-ninth adapter increment complete: the root `@blade-ai/agent-sdk` entry now owns subagent compatibility exports through package-local `src/subagents/*` instead of re-exporting legacy root `src/agent/subagents/*`. The package-local registry preserves builtin/session registration, directory loading, Claude-style permission mode mapping, source grouping, and plugin cleanup; `SubagentExecutor` is now a runner-injectable compatibility facade so the root entry no longer drags the legacy Agent runtime through the subagent export path.
- Thirtieth adapter increment complete: `@blade-ai/agent-sdk/session` now owns the session-first API control flow through a package-local `SessionRuntimeFactory` seam. `createSession`, `resumeSession`, `forkSession`, and `prompt` route through the local factory, `resumeSession`/`forkSession` enforce persistence guards in the package entry, and tests can inject a factory with `setSessionRuntimeFactory()` while the default factory temporarily adapts the legacy root session runtime until the full session implementation is migrated.
- Thirty-first adapter increment complete: `@blade-ai/agent-sdk/local` now owns its Node-local public runtime aggregation through package-local adapter seams (`mcp`, `memory`, `sandbox`, and `builtin-tools`). The `local/index.ts` entry no longer imports legacy root modules directly; it re-exports from package-local adapter files so MCP, filesystem memory, sandbox, builtin tools, and memory tools can be migrated independently without changing the public `local` entry shape.
- Thirty-second adapter increment complete: `@blade-ai/agent-sdk/local` now owns the filesystem memory runtime locally. `MemoryType`, `MemoryInput`, `Memory`, `MemoryStore`, `FileSystemMemoryStore`, and `MemoryManager` live in package-local `src/local/memory.ts`, preserving slug validation, `MEMORY.md` index maintenance, deterministic listing/search, and index rendering without importing legacy root `src/memory`.
- Thirty-third adapter increment complete: `@blade-ai/agent-sdk/local` now owns the memory read/write tools locally. `createMemoryReadTool()` and `createMemoryWriteTool()` use the package-local tool authoring runtime plus package-local `MemoryManager`; broader file/shell/search/task/todo/web/plan/system builtin suites remain staged for later package-local migration.
- Thirty-fourth adapter increment complete: `@blade-ai/agent-sdk/local` now owns the sandbox runtime locally. `SandboxExecutor`, `SandboxService`, their singleton helpers, command checks, sandbox capability detection, execution option building, and command wrapping live in package-local `src/local/sandbox.ts` without importing legacy root `src/sandbox`.
- Thirty-fifth adapter increment complete: `@blade-ai/agent-sdk/local` now owns the in-process MCP facade locally. `tool()` and `createSdkMcpServer()` live in package-local `src/local/mcp.ts`, use the MCP SDK directly, and no longer import legacy root `src/mcp` for local MCP server construction.
- Thirty-sixth adapter increment complete: `@blade-ai/agent-sdk/local` now owns the builtin tools entry locally. `getBuiltinTools()` no longer re-exports legacy root builtin registration; its current package-local baseline returns no tools by default and registers package-local memory read/write tools when a `MemoryManager` is provided, while the broader builtin suites remain staged for later adapter migrations.
- Thirty-seventh adapter increment complete: `@blade-ai/agent-sdk/session` now owns the JSONL session store implementation locally. `JsonlSessionStore`, `NoopSessionStore`, session snapshots, summaries, timelines, tool-call state, and subagent refs can be rebuilt from the existing unified JSONL event format without importing legacy root `src/session/SessionStore` or root context storage helpers, preparing the package-local `Session` runtime migration.
- Thirty-eighth adapter increment complete: `@blade-ai/agent-sdk/session` now owns session model/runtime config construction locally. `buildSessionModelConfig()` and `buildBladeConfig()` convert public `SessionOptions` into package-local `ModelConfig`/`BladeConfig`, preserving provider mapping, OpenAI organization/project headers, default base URLs, sampling, max output/context tokens, provider options, and thinking options without calling legacy root `Session` private helpers.
- Thirty-ninth adapter increment complete: `@blade-ai/agent-sdk/session` now has a package-local `src/session/Session.ts` lifecycle entry for `createSession`, `resumeSession`, `forkSession`, and `prompt`. The default `SessionRuntimeFactory` depends on this package-local entry instead of importing legacy root `src/session/Session` directly, leaving the legacy root session runtime isolated behind a single temporary adapter while the package-local `Session` implementation is migrated incrementally.
- Fortieth adapter increment complete: `@blade-ai/agent-sdk/session` now owns the public `prompt()` lifecycle locally. The package-local lifecycle creates a session through the injected factory, sends the user message, consumes stream events into `PromptResult` tool calls, usage, duration, and turn count, and closes the session in a `finally` block; the legacy adapter no longer imports the root `prompt()` helper.
- Forty-first adapter increment complete: `@blade-ai/agent-sdk/session` now owns the public `forkSession()` lifecycle locally. The package-local lifecycle resumes the source session through the injected factory, calls the live session's `fork({ messageId })`, and closes the source session in a `finally` block; the legacy adapter no longer imports the root `forkSession()` helper.
- Forty-second adapter increment complete: the public `@blade-ai/agent-sdk/session` entry now routes all four session-first API functions (`createSession`, `resumeSession`, `forkSession`, and `prompt`) through package-local lifecycle functions in `src/session/Session.ts`. `createSession()` and `resumeSession()` remain thin wrappers over the injected runtime primitive for now, but the public entry no longer bypasses the package-local lifecycle boundary.
- Forty-third adapter increment complete: `@blade-ai/agent-sdk/session` now owns the `persistSession: false` resume/fork guards inside the package-local lifecycle facade instead of the public entry wrapper. The public session entry now only injects the configured runtime factory into `createSession`, `resumeSession`, `forkSession`, and `prompt`, while lifecycle invariants live with the package-local session control flow.
- Forty-fourth adapter increment complete: `SessionRuntimeFactory` is now a narrow primitive port with only `create()` and `resume()`. Package-local lifecycle code owns `forkSession()` and `prompt()` composition, and both the default runtime factory and the legacy root adapter no longer expose `fork()` or `prompt()` escape hatches that could bypass the session lifecycle facade.
- Forty-fifth adapter increment complete: the default `@blade-ai/agent-sdk/session` runtime factory now lazy-loads the temporary legacy root session adapter on first `create()`/`resume()` use instead of statically importing it. This keeps the public session entry on package-local lifecycle code during module load and narrows the remaining root implementation dependency to a deferred server-runtime boundary.
- Forty-sixth adapter increment complete: the temporary legacy root session adapter remains behind a lazy boundary, and package verification now inspects the packed public `dist/session/index.js` static import closure to ensure legacy root session runtime content is not eagerly bundled into the session entry. Dynamic loading of the temporary adapter remains allowed until the package-local `Session` runtime fully replaces it.
- Forty-seventh adapter increment complete: the temporary legacy root session adapter now uses an explicit typed option bridge instead of `as never` / `as unknown as ISession` casts. Public session IDs are converted through the legacy branded `SessionId` constructor, and the remaining MCP handle compatibility cast is isolated to the `mcpServers` field until the package-local session runtime removes the legacy adapter entirely.
- Forty-eighth adapter increment complete: the temporary legacy root session adapter no longer imports legacy root `src/session/types.ts`; it infers the create/resume option shapes from the legacy functions it calls. This keeps the bridge coupled only to the legacy entrypoint while package-local session contracts continue to own the public API surface.
- Forty-ninth adapter increment complete: the temporary legacy root session adapter no longer imports legacy root branded type helpers. The resume bridge now casts only the `sessionId` field against the inferred legacy resume option type, leaving `src/session/Session.ts` as the adapter's only remaining root implementation import.
- Fiftieth adapter increment complete: topology protection for the temporary legacy root session adapter now uses an explicit root-import allowlist. Until the package-local `Session` runtime fully replaces the adapter, the adapter may only import the legacy root `src/session/Session.ts` entrypoint and cannot grow new direct dependencies on broader root implementation modules.
- Fifty-first adapter increment complete: `@blade-ai/agent-sdk/session` now owns the pure user-message content helpers locally. Text extraction, image counting, and JSON-or-string tool argument parsing live in `packages/agent-sdk/src/session/content.ts` against package-local contracts, preparing the package-local `Session` runtime to stop depending on private helpers in legacy root `src/session/Session.ts`.
- Fifty-second adapter increment complete: `@blade-ai/agent-sdk/session` now owns the pending-turn state machine locally. `PendingTurnBuffer` preserves the session-first `send()` before `stream()` contract, rejects overlapping pending messages, consumes turns atomically, and exposes cleanup for close/abort paths without depending on legacy root `Session` fields.
- Fifty-third adapter increment complete: `@blade-ai/agent-sdk/session` now owns turn abort-scope management locally. `TurnAbortController` creates per-turn abort signals, composes optional caller signals with the session abort signal, disconnects listeners during cleanup, and clears active turn state on external or session abort without depending on legacy root `Session` private methods.
- Fifty-fourth adapter increment complete: `@blade-ai/agent-sdk/session` now owns prompt stream event accumulation locally. `PromptStreamAccumulator` converts session `StreamMessage` events into `PromptResult` state for tool calls, usage, turn count, final content, and stream errors, and the package-local `prompt()` lifecycle now delegates to that accumulator instead of carrying inline event-reduction logic.
- Fifty-fifth adapter increment complete: `@blade-ai/agent-sdk/session` now owns lifecycle cleanup error preservation locally. `closeSessionAfterLifecycle()` keeps prompt/fork primary errors from being masked by cleanup failures while still surfacing pure close failures, and the package-local `prompt()` and `forkSession()` lifecycles now use it for session cleanup.
- Fifty-sixth adapter increment complete: `@blade-ai/agent-sdk/session` now owns the package-local session lifecycle state shell. `SessionLifecycleState` centralizes open/closed guards, idempotent close cleanup, pending-turn clearing, and active-turn aborting so the future package-local `Session` class can compose migrated state helpers without depending on legacy root `Session` fields.
- Fifty-seventh adapter increment complete: `@blade-ai/agent-sdk/session` now owns package-local turn preparation. `SessionTurnController` composes lifecycle guards, pending-turn buffering, abort-scope creation, and package-local context snapshot construction so the future package-local `Session` class can prepare `send()` / `stream()` turns without root `createContextSnapshot()` or legacy private pending fields.
- Fifty-eighth adapter increment complete: `@blade-ai/agent-sdk` now owns the trace recorder runtime locally. Package-local `TraceRecorder` records stream events, tool calls, usage, hook spans, and final results into the shared `AgentTrace` contract while preserving safe payload redaction by default, preparing the package-local session runtime to stop importing legacy root observability behavior.
- Fifty-ninth adapter increment complete: `@blade-ai/agent-sdk/session` now owns session trace lifecycle management locally. `SessionTraceManager` creates package-local trace recorders with session metadata and initial prompt events, keeps the bounded trace history, and isolates trace sink failures from the session turn lifecycle without depending on legacy root `Session` private methods.
- Sixtieth adapter increment complete: `@blade-ai/agent-sdk/session` now owns trace finalization locally. `createSessionTraceFinalizer()` closes a recorder at most once, remembers the completed trace, and notifies the trace sink through `SessionTraceManager`, preparing the package-local stream runtime to replace the legacy `finishTrace` closure.
- Sixty-first adapter increment complete: `@blade-ai/agent-sdk/session` now owns the legacy agent-event to session-stream projection locally. `LegacyStreamEventProjector` maps legacy turn/content/thinking/tool/usage events into package-local `StreamMessage` contracts while updating trace spans, tool-call records, and usage state, preparing the future package-local stream runtime to replace the large legacy `Session.stream()` switch.
- Sixty-second adapter increment complete: `@blade-ai/agent-sdk/session` now owns TaskCompleted hook reporting locally. `reportSessionTaskCompleted()` builds hook payloads from package-local user-message content helpers, including multimodal text/image metadata, so the future package-local stream runtime can report task completion without legacy `Session` private helpers.
- Sixty-third adapter increment complete: `@blade-ai/agent-sdk/session` now owns stream-result completion locally. `completeSessionStreamResult()` converts legacy loop results into final session usage/result/error messages, TaskCompleted reporting, and trace finalization without depending on legacy `Session.stream()` private branches.
- Sixty-fourth adapter increment complete: `@blade-ai/agent-sdk/session` now owns prompt-submit hook application locally. `applySessionPromptSubmit()` attaches the trace collector, applies UserPromptSubmit hooks with the active abort signal, and converts hook failures into finalized error traces plus session error messages without depending on legacy `Session.stream()` start branches.
- Sixty-fifth adapter increment complete: `@blade-ai/agent-sdk/session` now owns the package-local legacy stream turn runner. `runLegacySessionStreamTurn()` composes prompt-submit hooks, legacy event projection, stream-result completion, and final collector cleanup behind a package-local seam so the future package-local `Session.stream()` no longer needs the large inline legacy event loop.
- Sixty-sixth adapter increment complete: `@blade-ai/agent-sdk/session` now has a package-local session instance shell. `PackageLocalSession` composes the local pending-turn buffer, abort-scope controller, lifecycle state, and turn preparation with an injected stream runner, giving the default runtime factory a concrete package-local `ISession` carrier for replacing the temporary legacy adapter.
- Sixty-seventh adapter increment complete: `@blade-ai/agent-sdk/session` now has a package-local runtime factory seam for `PackageLocalSession`. `createPackageLocalSessionRuntimeFactory()` creates/resumes package-local session instances from injected id generation, turn id generation, stream runners, and cleanup hooks, preparing the default runtime factory to stop returning legacy root `Session` objects.
- Sixty-eighth adapter increment complete: `PackageLocalSession` now passes a session-level stream context into injected stream runners. Runners receive the package-local `sessionId` and original `SessionOptions` alongside each prepared active turn, giving the next default-factory slice enough context to bridge package-local session instances to the migrated legacy stream runner without reaching back into root `Session`.
- Sixty-ninth adapter increment complete: `@blade-ai/agent-sdk/session` now owns a package-local legacy stream bridge. `createLegacyStreamTurnBridge()` adapts `PackageLocalSession` turns into the migrated legacy stream runner through an injected driver for turn preparation, traces, hooks, and agent streaming, giving the default runtime factory a package-local target before the temporary root `Session` adapter is removed.
- Seventieth adapter increment complete: `@blade-ai/agent-sdk/session` now owns a package-local legacy runtime factory assembly seam. `createPackageLocalLegacySessionRuntimeFactory()` composes the package-local session factory with the package-local legacy stream bridge, so the future default factory can create `PackageLocalSession` instances from injected legacy drivers instead of returning legacy root `Session` objects.
- Seventy-first adapter increment complete: `PackageLocalSession` now has a package-local compatibility delegate seam for the session surface that is not migrated yet. Messages, default context, permission/model/max-turn setters, supported models, MCP status/actions, fork, trace reads, close, and abort can be forwarded to an injected delegate while `send()`/`stream()` remain owned by the package-local turn controller.
- Seventy-second adapter increment complete: the temporary legacy session adapter now returns package-local `PackageLocalSession` carriers instead of exposing legacy root `Session` objects directly. `createLegacyDelegateSession()` wraps the root session as a compatibility delegate and stream source, so the default runtime factory moves one step closer to owning the public session instance shape inside `@blade-ai/agent-sdk/session`.
- Seventy-third adapter increment complete: the temporary legacy session adapter no longer statically imports the legacy root `src/session/Session.ts` module. It keeps the remaining compatibility dependency behind a cached dynamic import used only by create/resume, further narrowing the package-local session entry's static runtime closure while the adapter is being replaced.
- Seventy-fourth adapter increment complete: the temporary legacy session adapter now exposes an explicit `LegacySessionModulePort` and accepts an injected `loadLegacySessionModule()` plus turn-id factory. The default adapter still dynamically loads the root legacy session module, but the compatibility dependency is now a narrow package-local port that can be replaced by a package-local driver without changing the public session factory.
- Seventy-fifth adapter increment complete: the default package-local session runtime factory now accepts an injected legacy runtime factory loader while preserving the default lazy adapter import. This moves the final compatibility boundary one level outward, so future server/local assembly can replace the temporary legacy adapter without changing package-local session lifecycle code.
- Seventy-sixth adapter increment complete: `@blade-ai/agent-sdk/session` now owns the first package-local runtime shell. `PackageLocalSessionRuntime` derives session storage root, project cwd, hooks, and configured MCP server state locally, and `isPackageLocalSdkMcpServerHandle()` replaces the root runtime's in-process MCP handle guard as the package-local runtime migration anchor.
- Seventy-seventh adapter increment complete: `PackageLocalSessionRuntime` now owns session create/load lifecycle semantics through an injected `PackageLocalRuntimeSessionStorePort`. `ensureSessionCreated()` and `ensureSessionLoaded()` mirror the root runtime's create-or-load behavior without importing root `ContextManager`, preparing the package-local runtime shell to take over persistence wiring incrementally.
- Seventy-eighth adapter increment complete: `PackageLocalSessionRuntime` now owns turn workspace preparation through an injected `PackageLocalRuntimeWorkspacePort`. `prepareTurn(snapshot)` mirrors the root runtime's project path and environment update semantics, including `cwd` normalization, without importing root workspace/context managers.
- Seventy-ninth adapter increment complete: `PackageLocalSessionRuntime` now owns close lifecycle semantics through an injected `PackageLocalRuntimeMcpRegistryPort`. `close()` mirrors the root runtime's MCP disconnect-all behavior without importing root MCP registry implementation.
- Eightieth adapter increment complete: `PackageLocalSessionRuntime` now owns MCP read-side capability projection through the package-local MCP registry port. `mcpCapabilities()` and `mcpServerStatus()` expose package-local capability/status contracts without importing root `projectMcpCapabilities()` or the root MCP registry implementation.
- Eighty-first adapter increment complete: `PackageLocalSessionRuntime` now owns MCP tool-list projection through package-local capabilities. `mcpListTools()` derives public tool info from the same package-local capability contract instead of calling through the root MCP registry.
- Eighty-second adapter increment complete: `PackageLocalSessionRuntime` now owns MCP action lifecycle sequencing through the package-local MCP registry port. `mcpConnect()`, `mcpDisconnect()`, and `mcpReconnect()` perform package-local configured-server lookup, call injected registry actions, and trigger injected tool refreshes without importing root MCP or tool registry implementations.
- Eighty-third adapter increment complete: `PackageLocalSessionRuntime` now owns tool filtering semantics locally. `filterTools()` preserves the security-sensitive `allowedTools !== undefined` behavior, so `allowedTools: []` disables all tools while disallow rules still apply after allowlist filtering.
- Eighty-fourth adapter increment complete: `PackageLocalSessionRuntime` now owns filtered tool registration through an injected `PackageLocalRuntimeToolCatalogPort`. `registerTools()` applies the package-local allow/deny filtering before touching the catalog and skips empty registrations, keeping local/builtin/custom tool assembly in `@blade-ai/agent-sdk` instead of the runtime-independent `@blade-ai/agent` core.
- Eighty-fifth adapter increment complete: `PackageLocalSessionRuntime` now owns configured MCP server registration through package-local registry and logger ports. `registerConfiguredMcpServers()` registers in-process handles, skips disabled remote configs, isolates remote registration failures through `PackageLocalRuntimeLoggerPort`, and refreshes configured server tool state without importing the legacy root MCP registry.
- Eighty-sixth adapter increment complete: `PackageLocalSessionRuntime` now owns MCP tool refresh into the package-local tool catalog. `refreshMcpTools()` removes stale MCP tools per server, reads available tools through `getAvailableToolsByServerNames()`, applies the package-local allow/deny filter, derives stable MCP `sourceId` metadata from tags or legacy names, and registers filtered tools as `kind: "mcp"` / `trustLevel: "remote"` without delegating catalog writes to the legacy root runtime.
- Eighty-seventh adapter increment complete: `PackageLocalSessionRuntime` now owns custom tool registration sequencing through an injected `PackageLocalRuntimeCustomToolFactoryPort`. `registerCustomTools()` skips empty session tool lists, converts configured `SessionOptions.tools` through the package-local factory seam, applies the same allow/deny filtering path as other tool sources, and registers surviving tools as `kind: "custom"` / `trustLevel: "workspace"` / `sourceId: "session"`.
- Eighty-eighth adapter increment complete: `PackageLocalSessionRuntime` now owns builtin tool registration sequencing through an injected `PackageLocalRuntimeBuiltinToolProviderPort`. `registerBuiltinTools()` asks the adapter for builtin tools with session id, storage root, MCP registry, and MCP-protocol-tool policy context, then applies the same allow/deny filtering path and registers surviving tools as trusted builtin catalog entries.
- Eighty-ninth adapter increment complete: `PackageLocalSessionRuntime` now owns subagent initialization sequencing through an injected `PackageLocalRuntimeSubagentRegistryPort`. `initializeSubagents()` wires the package-local logger and project directory, loads standard subagent locations from project/storage roots, and converts `SessionOptions.agents` into session-source subagent configs without importing the legacy root subagent registry.
- Ninetieth adapter increment complete: `PackageLocalSessionRuntime` now owns permission handler composition through an injected `PackageLocalRuntimePermissionHookPort` and package-local permission factories. `createPermissionHandler()` composes PermissionRequest hook input rewrites, explicit `permissionHandler` / `canUseTool` callbacks, and the existing fallback ask behavior without importing the legacy root runtime.
- Ninety-first adapter increment complete: `PackageLocalSessionRuntime` now owns hook manager initialization through an injected `PackageLocalRuntimeHookManagerPort`. `initializeHooks()` enables the package-local hook manager only when session hook callbacks are configured, preserving the legacy activation semantics without importing the root `HookManager` singleton.
- Ninety-second adapter increment complete: `PackageLocalSessionRuntime` now owns execution pipeline assembly through an injected `PackageLocalRuntimeExecutionPipelineFactoryPort`. `createExecutionPipeline()` builds package-local permission config defaults, resolves the session permission mode, composes the package-local permission handler, and passes logger/catalog seams to the factory without importing the legacy root `ExecutionPipeline` class.
- Ninety-third adapter increment complete: `PackageLocalSessionRuntime` now owns package-local agent runtime dependency projection through `PackageLocalAgentRuntimeDeps`. `getAgentRuntimeDeps()` returns the cached execution pipeline, default context, MCP registry, subagent registry, background agent manager, hook runtime, runtime-managed flag, and logger from injected package-local ports instead of relying on the legacy root `SessionRuntime` accessor.
- Ninety-fourth adapter increment complete: `PackageLocalSessionRuntime` now owns package-local kernel port factory seams. `getKernelToolPort()`, `getKernelStorePort()`, `getKernelTracePort()`, and `getKernelHookPort()` create `@blade-ai/agent` tool/store/trace/hook ports through an injected `PackageLocalRuntimeKernelPortFactoryPort`, forwarding SDK-local catalog, pipeline, store, hook runtime, trace recorder, and execution-context dependencies without importing the legacy root kernel adapters.
- Ninety-fifth adapter increment complete: `PackageLocalSessionRuntime` now owns package-local agent kernel assembly through injected model resolver and kernel factory ports. `createAgentKernel()` resolves explicit or configured model ports, composes store/hook/trace/tool ports from package-local seams, forwards max-step and model request defaults, and avoids directly constructing the runtime-independent `AgentKernel` inside the SDK runtime shell.
- Ninety-sixth adapter increment complete: `PackageLocalSessionRuntime` now owns package-local kernel stream event projection. `projectKernelEventToStreamMessages()` maps `@blade-ai/agent` content, thinking, tool use/result, permission update, usage, result, and error events into SDK `StreamMessage` objects while preserving session ids, context-token usage metadata, optional thinking visibility, and permission rule updates without importing the legacy root `SessionRuntime`.
- Ninety-seventh adapter increment complete: `PackageLocalSessionRuntime` now owns package-local kernel turn streaming. `streamAgentKernelTurn()` resolves the session kernel model once, creates a runnable `PackageLocalRuntimeAgentKernelPort`, emits the session `turn_start`, runs the kernel with input/turn/signal data, and projects kernel events through the package-local stream projection seam without importing the legacy root `SessionRuntime` stream loop.
- Ninety-eighth adapter increment complete: `@blade-ai/agent-sdk/session` now owns a package-local kernel stream bridge. `createKernelStreamTurnBridge()` adapts `PackageLocalSession` turns into `PackageLocalSessionRuntime.streamAgentKernelTurn()`, prepares the runtime workspace snapshot, extracts text from multimodal user messages through package-local content helpers, maps session max-turn settings to kernel max steps, and supplies session execution context without importing legacy root stream code.
- Ninety-ninth adapter increment complete: `@blade-ai/agent-sdk/session` now owns a package-local kernel runtime factory assembly. `createPackageLocalKernelSessionRuntimeFactory()` composes `createPackageLocalSessionRuntimeFactory()` with the kernel stream bridge, preserving create/resume context propagation, package-local `PackageLocalSession` construction, stream wiring, and cleanup hooks without using the temporary legacy stream bridge.
- One hundredth adapter increment complete: the default `@blade-ai/agent-sdk/session` runtime factory now has a kernel-first factory loader seam. `createDefaultSessionRuntimeFactory()` can route create/resume through an injected package-local kernel runtime factory before falling back to the temporary legacy adapter, giving server/local assembly a clean replacement point for removing the legacy root session dependency.
- One hundred first adapter increment complete: `@blade-ai/agent-sdk/session` now owns the default package-local kernel runtime factory skeleton. `createDefaultKernelSessionRuntimeFactory()` assembles `createPackageLocalKernelSessionRuntimeFactory()`, `PackageLocalSessionRuntime`, and `buildBladeConfig()` from session options, exposes typed runtime-port injection for server/local assembly, forwards session default context, and closes the package-local runtime during session cleanup without importing legacy root session code.
- One hundred second adapter increment complete: `@blade-ai/agent-sdk/session` now owns package-local kernel model resolution. `createPackageLocalKernelModelResolver()` resolves `BladeConfig` model ids/names/model strings into `@blade-ai/ai` `ModelPort` instances through the public Vercel provider adapter, preserves sampling/context/provider defaults for `AgentKernel`, and is installed as the default resolver for `createDefaultKernelSessionRuntimeFactory()` without importing the legacy root `ModelManager`.
- One hundred third adapter increment complete: `@blade-ai/agent-sdk/session` now owns the package-local agent kernel factory adapter. `createPackageLocalAgentKernelFactory()` constructs runtime-independent `AgentKernel` instances from `@blade-ai/agent`, and `createDefaultKernelSessionRuntimeFactory()` installs it by default while still allowing server/local runtimes to inject a custom kernel factory.
- One hundred fourth adapter increment complete: the default `@blade-ai/agent-sdk/session` runtime factory is now kernel-only. `createDefaultSessionRuntimeFactory()` directly assembles `createDefaultKernelSessionRuntimeFactory()` with session id, turn id, and runtime-port options, while retaining an explicit kernel factory loader seam for tests and alternate server/local assembly without dynamically importing the legacy root session adapter.
- One hundred fifth adapter increment complete: the temporary root-loading `legacySessionAdapter.ts` has been removed from `@blade-ai/agent-sdk/session`. The package no longer carries a default-path bridge that dynamically imports legacy root `src/session/Session.js`, leaving the remaining package-local legacy stream helpers isolated for explicitly tested adapter behavior.
- One hundred sixth adapter increment complete: package-local legacy stream helpers have been retired from `@blade-ai/agent-sdk/session`. `legacyStreamEvents.ts`, `legacyStreamRunner.ts`, `legacyStreamBridge.ts`, `packageLocalLegacyRuntimeFactory.ts`, and `legacySessionDelegate.ts` were removed, and the public package-local `StreamOptions` contract now exposes only `includeThinking` because the session runtime is kernel-only.
- One hundred seventh adapter increment complete: packed `@blade-ai/agent-sdk/session` declarations are now guarded against retired legacy stream/runtime contracts. Package verification requires `dist/session/types.d.ts` in the tarball and rejects `runtime?: 'kernel' | 'legacy'`, `experimentalKernel`, `legacyStream`, and `packageLocalLegacy` markers so the public session-first type surface cannot silently regress to migration-era escape hatches.
- One hundred eighth adapter increment complete: the legacy stream escape hatch has been retired from the remaining root session source. Root `StreamOptions` now exposes only `includeThinking`, `Session.stream()` always routes through the kernel turn bridge, and root session context, in-memory history, and observability tests now exercise the kernel-default path instead of the old `Agent.streamChat()` loop.
- One hundred ninth adapter increment complete: the default package-local kernel stream bridge now ensures runtime capabilities are initialized before a kernel turn runs. Configured MCP servers are registered and refreshed, session custom tools and builtin tools are registered, subagents are initialized, and configured hook managers are enabled before `AgentKernel.runTurn()` receives the turn.
- One hundred tenth adapter increment complete: the default package-local session runtime now owns a lightweight hook runtime that converts configured `UserPromptSubmit` callbacks into an `AgentHookPort.beforeModel()` rewrite for the first model step. Session-first `createSession({ hooks })` can now rewrite the prompt on the default kernel path without relying on the legacy root `HookRuntime`.
- One hundred eleventh adapter increment complete: package-local session control methods now synchronize with the default kernel runtime. `setModel()` updates the runtime `BladeConfig` before the next kernel turn resolves its model, and `setPermissionMode()` / `setMaxTurns()` flow through the same runtime control port instead of only mutating the facade-side session options.
- One hundred twelfth adapter increment complete: package-local permission-mode changes now invalidate the cached execution pipeline. After `setPermissionMode()` the next tool execution pipeline is rebuilt with the current mode, preventing session-first permission policy changes from being hidden behind a stale runtime cache.
- One hundred thirteenth adapter increment complete: `setDefaultContext()` now synchronizes from the package-local session facade into the default kernel runtime. Agent runtime dependency projection reads the current default context through a runtime getter, so session-first context changes are visible to runtime-managed helpers instead of being frozen at session construction.
- One hundred fourteenth adapter increment complete: default-context project path derivation now updates with `setDefaultContext()`. Package-local subagent initialization reads the current runtime project path through a getter, so standard subagent location discovery follows the latest session-first workspace context instead of the construction-time cwd.
- One hundred fifteenth adapter increment complete: default-context changes now refresh package-local subagent standard-location discovery after runtime capabilities have already been initialized. The runtime keeps the broader capability initialization cache intact, but marks subagent locations dirty after `setDefaultContext()` so the next kernel turn sees the latest session-first workspace cwd.
- One hundred sixteenth adapter increment complete: package-local runtime capability initialization can now recover from failed attempts. A rejected initialization clears the cached promise and initialized marker, allowing the next kernel turn to retry builtin/custom/MCP/subagent/hook setup instead of replaying a stale failure forever.
- One hundred seventeenth adapter increment complete: default-context changes during in-flight runtime capability initialization now also mark subagent standard-location discovery dirty. If subagent discovery already ran with the old cwd before initialization settles, the next ensure pass refreshes it with the latest session-first workspace context instead of missing the update window.
- One hundred eighteenth adapter increment complete: package-local runtime capability initialization state now lives in `runtimeCapabilities.ts` instead of on `PackageLocalSessionRuntime`. The runtime class delegates ensure/retry/subagent-refresh state transitions through an operations seam, while topology guards prevent the promise cache and dirty-location markers from drifting back into the class.
- One hundred nineteenth adapter increment complete: package-local runtime control setters now live in `runtimeControls.ts` instead of on `PackageLocalSessionRuntime`. The runtime class delegates default-context, permission-mode, model, and max-turn state updates through an operations seam, while focused tests and topology guards preserve session-first setter behavior without reintroducing inline control logic.
- One hundred twentieth adapter increment complete: package-local session lifecycle operations now own session start/end hook dispatch and close-resource finalization. `PackageLocalSessionRuntime` delegates `runSessionStart()` and `close()` through the lifecycle operations seam, preserving resume payloads and MCP cleanup ordering while topology guards keep direct hook calls out of the runtime class.
- One hundred twenty-first adapter increment complete: package-local runtime port contracts now live in `runtimePorts.ts` instead of being declared on `PackageLocalSessionRuntime`. The runtime class imports and re-exports those contracts for existing package-local consumers, while default runtime assembly and noop port resolution can depend on the port contract module without coupling to the runtime class implementation.
- One hundred twenty-second adapter increment complete: package-local runtime initial state derivation now lives in `runtimeState.ts` instead of in the `PackageLocalSessionRuntime` constructor. Focused tests prove storage-root override and normalization, project cwd precedence, and hook callback defaults without runtime class state, while topology guards prevent reintroducing inline initial-state assignment.
- One hundred twenty-third adapter increment complete: package-local runtime capability startup sequencing now lives in `runtimeCapabilities.ts` instead of a private method on `PackageLocalSessionRuntime`. Focused tests prove MCP registration, custom tools, builtin tools, subagents, and hooks run in dependency order and stop on early async failure, while topology guards prevent reintroducing the inline startup method.
- One hundred twenty-fourth adapter increment complete: package-local runtime capability startup and initialization are now assembled through a single `createPackageLocalRuntimeCapabilityOperations()` bundle. Focused tests prove the bundle preserves startup ordering and post-initialization subagent refresh semantics, while topology guards keep the runtime class from directly wiring the lower-level capability factories.
- One hundred twenty-fifth adapter increment complete: package-local MCP capability, server, and tool refresh operations are now retained in `PackageLocalSessionRuntime` as one `PackageLocalRuntimeMcpOperations` bundle instead of five private child fields. Topology guards keep the runtime class on the bundled MCP seam while preserving session-first MCP status, connect/disconnect/reconnect, registration, and tool refresh delegation.
- One hundred twenty-sixth adapter increment complete: package-local session lifecycle and workspace operations are now retained in `PackageLocalSessionRuntime` as one `PackageLocalRuntimeSessionOperations` bundle instead of separate lifecycle and workspace fields. Topology guards keep session creation/loading/start/end, close cleanup, and turn workspace preparation delegated through the bundled session seam.
- One hundred twenty-seventh adapter increment complete: package-local execution pipeline cache and agent runtime dependency projection are now retained in `PackageLocalSessionRuntime` as one `PackageLocalRuntimeExecutionOperations` bundle instead of separate child fields. Topology guards keep pipeline reset/get and runtime dependency projection delegated through the bundled execution seam.
- One hundred twenty-eighth adapter increment complete: package-local runtime bootstrapping now lives in `runtimeBootstrap.ts` instead of being assembled directly in the `PackageLocalSessionRuntime` constructor. Focused tests prove initial state derivation and port resolution can be exercised together without constructing a session runtime, while topology guards keep the runtime class on the bootstrap seam instead of directly calling initial-state and noop-port factories.
- One hundred twenty-ninth adapter increment complete: package-local runtime port-field projection now lives in `runtimePortProjection.ts` instead of expanding resolved ports directly inside the `PackageLocalSessionRuntime` constructor. Focused tests prove resolved ports plus option-provided custom and builtin tool adapters can be projected without constructing a session runtime, while topology guards keep the constructor from reintroducing direct resolved-port field assignment.
- One hundred thirtieth adapter increment complete: package-local session/MCP connection operations now live in `runtimeConnectionOperations.ts` instead of being separately wired inside the `PackageLocalSessionRuntime` constructor. Focused tests prove session lifecycle close delegates MCP shutdown, while MCP tool refresh and configured-server access remain available through the same injected ports without constructing a session runtime.

### Phase 5: Production Verification Chain

Objective: Make quality gates broad enough for a production agent SDK.

Deliverables:

- `pnpm run verify`
- `pnpm run verify:packages`
- `pnpm run verify:entrypoints`
- `pnpm run test:unit`
- `pnpm run test:integration`
- `pnpm run test:live:glm`
- temporary consumer install test from packed tarballs
- package boundary scanner
- browser bundle scanner
- documentation build gate

CI:

- CI runs lint, type-check, build, unit, integration, package verification, docs build.
- Live GLM tests stay manual or scheduled with secrets, not mandatory for every PR.

Status:

- First verification-chain increment complete: root `pnpm run verify` now aggregates lint, root and workspace type checks, package boundary checks, docs build, entrypoint/browser-safety checks, packed package smoke tests, unit tests, and integration tests. `pnpm run verify:packages` packs `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk`, checks required JS/declaration files and absence of test files, installs the tarballs into an external temporary consumer, and imports the public package/subpath exports. The release workflow now runs the same `pnpm run verify` gate before semantic-release.
- Second verification-chain increment complete: the package boundary verifier now rejects forbidden runtime dependencies declared in the `@blade-ai/agent` manifest, not just forbidden imports from source files, protecting the runtime-independent kernel boundary before code is imported.
- Third verification-chain increment complete: `pnpm run test:live:session-glm` now provides a dedicated session-first live smoke over the built `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` packages, while the heavier `test:integration` suite remains explicitly gated by `INTEGRATION_API_KEY` / `INTEGRATION_BASE_URL`.
- Fourth verification-chain increment complete: `pnpm run verify:packages` now inspects packed declaration file contents for critical ownership regressions, currently preventing `@blade-ai/agent-sdk/session` from publishing a `dist/session/index.d.ts` that points back at the legacy root `./Session.js` declarations.
- Fifth verification-chain increment complete: `pnpm run verify:packages` now fresh-builds `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` before packing, and it rejects `@blade-ai/agent-sdk/tools` declaration regressions that point back at legacy root `./core/createTool.js` or `./catalog/index.js` declarations.
- Sixth verification-chain increment complete: `pnpm run verify:packages` now rejects root `@blade-ai/agent-sdk` declaration overlays that leak temporary `public-index.js` references or legacy root implementation declarations, and it rejects `@blade-ai/agent-sdk/local` declaration regressions that point back at root MCP, memory, sandbox, or builtin-tool declaration modules.
- Seventh verification-chain increment complete: `pnpm run verify:packages` now requires the packed `@blade-ai/agent-sdk` permission declarations, rejects public declaration maps for overlaid root/core/local/session/tools/permission entries, and prevents permission declarations from regressing to legacy `SensitiveFileDetector` or `ToolEffects` dependencies.
- Eighth verification-chain increment complete: `pnpm run verify:packages` now rejects packed `@blade-ai/agent-sdk/tools` runtime and declaration regressions that point back at legacy root `src/tools/core/createTool` or `src/tools/catalog/ToolCatalog`, keeping the public tool authoring entry package-local.
- Ninth verification-chain increment complete: `pnpm run verify:packages` now rejects packed root `@blade-ai/agent-sdk` runtime and declaration regressions that point back at legacy root `src/agent/subagents`, keeping root subagent compatibility exports package-local.
- Tenth verification-chain increment complete: `pnpm run verify:packages` now requires the packed `@blade-ai/agent-sdk/session` runtime factory declaration and rejects `dist/session/index.js` regressions that directly point back at legacy root `src/session/Session`, keeping the public session entry on the package-local factory seam.
- Eleventh verification-chain increment complete: `pnpm run verify:packages` now rejects packed `@blade-ai/agent-sdk/local` entry regressions where `dist/local/index.js` directly points back at legacy root `src/mcp`, `src/memory`, `src/sandbox`, or `src/tools/builtin`, while topology tests require package-local MCP, memory, sandbox, and builtin-tool adapter source files.
- Twelfth verification-chain increment complete: `pnpm run verify:packages` now rejects packed `@blade-ai/agent-sdk/local` memory declaration regressions that publish the earlier stub-style `read`/`write`/boolean-delete API instead of the package-local `save`/`get`/`list`/`delete` memory API.
- Thirteenth verification-chain increment complete: topology tests now require package-local memory tool sources and reject legacy root memory-tool imports from `local/builtin-tools.ts`; package verification also rejects packed `@blade-ai/agent-sdk/local` runtime regressions that point directly at legacy root memory tools.
- Fourteenth verification-chain increment complete: topology tests now reject legacy root sandbox imports from `local/sandbox.ts`, and package verification rejects packed local sandbox declarations that fall back to the old placeholder constructor-only shape.
- Fifteenth verification-chain increment complete: topology tests now reject legacy root MCP imports from `local/mcp.ts`, and package verification rejects packed local MCP declarations that fall back to the old `unknown[]` placeholder signatures.
- Sixteenth verification-chain increment complete: topology tests now reject legacy root builtin-tool imports from `local/builtin-tools.ts`, and package verification rejects packed local builtin declarations that fall back to the old `getBuiltinTools(...args: unknown[])` placeholder signature.
- Seventeenth verification-chain increment complete: topology tests now require package-local session store source ownership, and package verification requires the packed `@blade-ai/agent-sdk` session store declaration while rejecting declaration regressions that point back at root context storage or legacy root `SessionStore`.
- Eighteenth verification-chain increment complete: topology tests now require package-local session config source ownership, and package verification requires the packed `@blade-ai/agent-sdk` session config declaration while rejecting declaration regressions that point back at legacy root `Session` or non-package-local config types.
- Nineteenth verification-chain increment complete: topology tests now require `packages/agent-sdk/src/session/Session.ts` and reject default factory imports that bypass it. Package verification also requires the packed `dist/session/Session.d.ts` declaration and rejects declarations that expose legacy root `src/session/Session` contracts.
- Twentieth verification-chain increment complete: session factory tests now prove package-local `prompt()` is composed from `create`/`send`/`stream`/`close` instead of delegating to `SessionRuntimeFactory.prompt()`, and topology tests reject reintroducing the root `prompt` import in the legacy session adapter.
- Twenty-first verification-chain increment complete: session factory tests now prove package-local `forkSession()` is composed from `resume` plus the live session's `fork()` and source-session cleanup instead of delegating to `SessionRuntimeFactory.fork()`, and topology tests reject reintroducing the root `forkSession` import in the legacy session adapter.
- Twenty-second verification-chain increment complete: topology tests now reject public `session/index.ts` regressions where `createSession()` or `resumeSession()` call `sessionRuntimeFactory.create/resume` directly instead of entering the package-local lifecycle facade.
- Twenty-third verification-chain increment complete: session factory tests now call package-local lifecycle functions directly and prove `resumeSession()`/`forkSession()` reject `persistSession: false` before touching the runtime factory. Topology tests also reject moving those guard strings back into the public `session/index.ts` entry.
- Twenty-fourth verification-chain increment complete: topology tests now require `SessionRuntimeFactory` and the default runtime factory to stay limited to `create()`/`resume()` primitives, preventing future `fork()` or `prompt()` runtime hooks from bypassing package-local lifecycle ownership.
- Twenty-fifth verification-chain increment complete: package verification now rejects packed `@blade-ai/agent-sdk` session factory declarations that expose `fork(options)` or `prompt(message)`, so the published `SessionRuntimeFactory` contract remains a narrow `create()`/`resume()` primitive port.
- Twenty-sixth verification-chain increment complete: topology tests now reject static `legacySessionAdapter` imports from the default session runtime factory and require the adapter to be loaded through `import('./legacySessionAdapter.js')`, preserving a lazy boundary around the remaining root session implementation.
- Twenty-seventh verification-chain increment complete: `pnpm run verify:packages` now extracts the packed `@blade-ai/agent-sdk` tarball, walks the static import closure from `package/dist/session/index.js`, and rejects eager inclusion of legacy root session runtime markers while still permitting the temporary adapter to remain behind a dynamic `import()` boundary.
- Twenty-eighth verification-chain increment complete: topology tests now reject `as never` and `as unknown as ISession` in the temporary legacy session adapter, forcing the remaining bridge to stay typed and explicit while the package-local session implementation is migrated.
- Twenty-ninth verification-chain increment complete: topology tests now reject direct imports from legacy root `src/session/types.ts` inside the temporary session adapter, so adapter compatibility types must be inferred from the remaining legacy entrypoint rather than coupling to broader root session contracts.
- Thirtieth verification-chain increment complete: topology tests now reject direct imports from legacy root `src/types/branded.ts` inside the temporary session adapter, keeping the adapter's remaining root dependency focused on the legacy `Session` entrypoint.
- Thirty-first verification-chain increment complete: topology tests now extract every root `src/*` import from the temporary legacy session adapter and compare it to a single allowed import list, preventing future piecemeal legacy root dependencies from sneaking into the session bridge.
- Thirty-second verification-chain increment complete: focused package-local tests now cover session content helper behavior for string prompts, multimodal text/image parts, and JSON fallback parsing; topology also requires the helper source to remain package-local without root imports.
- Thirty-third verification-chain increment complete: focused package-local tests now cover pending-turn lifecycle behavior for missing sends, single-message consumption, overlapping send rejection, and cleanup. Topology also requires the pending-turn source to remain package-local without root imports.
- Thirty-fourth verification-chain increment complete: focused package-local tests now cover turn abort-scope behavior for local signals, external abort propagation, session abort propagation, cleanup listener disconnection, and overlapping turn rejection. Topology also requires the turn-abort source to remain package-local without root imports.
- Thirty-fifth verification-chain increment complete: focused package-local tests now cover prompt stream accumulation for successful results, tool result updates, stream errors, and result-error fallbacks. Topology also requires the prompt accumulator source to remain package-local without root imports and requires the package-local `prompt()` lifecycle to instantiate it.
- Thirty-sixth verification-chain increment complete: session factory lifecycle tests now prove prompt stream errors and fork errors remain the user-visible failure when cleanup also fails. Topology also requires the cleanup helper source to remain package-local without root imports and requires the package-local lifecycle to call it.
- Thirty-seventh verification-chain increment complete: focused package-local tests now cover session lifecycle state behavior for open/closed guards, close cleanup idempotence, pending-turn cleanup, and abort-without-close semantics. Topology also requires the lifecycle state source to remain package-local without root imports.
- Thirty-eighth verification-chain increment complete: focused package-local tests now cover turn preparation behavior for context snapshot merging, pending-turn consumption, active abort-scope creation, external abort propagation, and closed-session guards. Topology also requires the turn controller source to remain package-local without root imports.
- Thirty-ninth verification-chain increment complete: focused package-local tests now cover trace recorder behavior for unified stream/tool/usage/hook/result events, safe default redaction, explicit payload capture, and cloned trace reads. Topology also requires the recorder source to remain package-local without root imports.
- Fortieth verification-chain increment complete: focused package-local tests now cover session trace manager behavior for observability disabled mode, recorder creation with metadata and prompt events, bounded trace retention, and sink failure isolation. Topology also requires the trace manager source to remain package-local without root imports.
- Forty-first verification-chain increment complete: focused package-local tests now cover trace finalization behavior for disabled observability, single-finish idempotence, trace retention, sink notification, and sink failure isolation. Topology also requires the trace finalizer to remain in the package-local trace manager source.
- Forty-second verification-chain increment complete: focused package-local tests now cover legacy stream event projection for turn/content/thinking/tool lifecycle/tool side-effect/usage events, including trace recording and tool-call state updates. Topology also requires the projector source to remain package-local without root imports.
- Forty-third verification-chain increment complete: focused package-local tests now cover TaskCompleted hook reporting for text and multimodal prompts, proving task descriptions, image flags, image counts, summaries, and success flags are derived without legacy root helpers. Topology also requires the reporter source to remain package-local without root imports.
- Forty-fourth verification-chain increment complete: focused package-local tests now cover stream result completion for missing loop results, non-abort failures, successful completions, and aborted completions, including final session messages, TaskCompleted payloads, and trace status/data. Topology also requires the stream completion helper to remain package-local without root imports.
- Forty-fifth verification-chain increment complete: focused package-local tests now cover prompt-submit hook application for trace collector installation, abort-signal forwarding, rewritten prompts, hook failure error messages, trace finalization, and collector cleanup. Topology also requires the prompt-submit helper to remain package-local without root imports.
- Forty-sixth verification-chain increment complete: focused package-local tests now cover the composed legacy stream runner for rewritten prompts, legacy event projection, usage/result completion, prompt-submit failure short-circuiting, trace finalization, and collector cleanup. Topology also requires the legacy stream runner helper to remain package-local without root imports.
- Forty-seventh verification-chain increment complete: focused package-local tests now cover the session instance shell for pending-turn preparation, default/turn context merging, injected stream runner dispatch, missing pending-turn rejection, idempotent close cleanup, and closed-session guards. Topology also requires the session instance source to remain package-local without root imports.
- Forty-eighth verification-chain increment complete: focused package-local tests now cover the package-local runtime factory seam for generated create ids, supplied resume ids, injected stream runner dispatch, and session cleanup context propagation. Topology also requires the runtime factory seam to remain package-local without root imports.
- Forty-ninth verification-chain increment complete: focused package-local tests now cover stream-context propagation from `PackageLocalSession.stream()` into injected stream runners, proving each runner can observe the package-local session id and original session options without importing legacy root state.
- Fiftieth verification-chain increment complete: focused package-local tests now cover the legacy stream bridge for turn preparation, prompt-submit hooks, abort-signal/max-turn forwarding, thinking/content/usage/result projection, and trace finalization. Topology also requires the bridge source to remain package-local without root imports while reusing `runLegacySessionStreamTurn()`.
- Fifty-first verification-chain increment complete: focused package-local tests now cover the package-local legacy runtime factory assembly for create/resume context propagation, package-local session creation, legacy bridge streaming, and cleanup forwarding. Topology also requires the assembly source to remain package-local without root imports while composing `createPackageLocalSessionRuntimeFactory()` and `createLegacyStreamTurnBridge()`.
- Fifty-second verification-chain increment complete: focused package-local tests now cover the compatibility delegate seam for not-yet-migrated session behavior, including messages, context setters, permission/model/max-turn setters, supported models, MCP methods, fork, traces, close, and abort. Topology also requires the delegate seam to remain inside the package-local session instance source.
- Fifty-third verification-chain increment complete: focused package-local tests now cover wrapping a legacy `ISession` as a `PackageLocalSession` carrier, including deferred send/stream delegation and compatibility method forwarding. Topology also requires the temporary legacy adapter to use `createLegacyDelegateSession()` so it can no longer return root sessions directly.
- Fifty-fourth verification-chain increment complete: focused public lifecycle tests now cover the default `@blade-ai/agent-sdk/session` factory returning package-local `PackageLocalSession` carriers for both `createSession()` and `resumeSession()`, while keeping `send()` package-local until `stream()` delegates to the temporary legacy runtime.
- Fifty-fifth verification-chain increment complete: topology now forbids static root `from '../../../../src/*'` imports inside the temporary legacy session adapter and requires the remaining root session dependency to stay behind an explicit dynamic import until the adapter is removed.
- Fifty-sixth verification-chain increment complete: focused adapter tests now cover creating and resuming package-local sessions through an injected `LegacySessionModulePort`, proving the temporary adapter can be exercised without loading the legacy root runtime. Topology also rejects root-type-derived adapter option shapes, keeping the remaining root dependency at the dynamic default loader boundary.
- Fifty-seventh verification-chain increment complete: focused session factory tests now cover composing the default lifecycle around an injected legacy runtime factory loader, proving the outer package-local factory can be exercised without loading the temporary legacy adapter. Topology also requires the default factory injection seam to remain present.
- Fifty-eighth verification-chain increment complete: focused package-local runtime tests now cover storage-root normalization, project cwd derivation, hook callback ownership, and in-process MCP handle detection without importing root `src/session/SessionRuntime.ts`. Topology also requires the runtime shell to remain package-local and root-import-free.
- Fifty-ninth verification-chain increment complete: focused package-local runtime tests now cover `ensureSessionCreated()` and `ensureSessionLoaded()` against an injected store port, including load-miss create fallback and load-hit no-op behavior. Topology also requires the store port and lifecycle methods to remain on the package-local runtime shell.
- Sixtieth verification-chain increment complete: focused package-local runtime tests now cover `prepareTurn(snapshot)` against an injected workspace port, including project path forwarding and `cwd` environment normalization. Topology also requires the workspace port and prepare-turn method to remain package-local.
- Sixty-first verification-chain increment complete: focused package-local runtime tests now cover `close()` against an injected MCP registry port. Topology also requires the MCP registry port and close lifecycle method to remain on the package-local runtime shell.
- Sixty-second verification-chain increment complete: focused package-local runtime tests now cover `mcpCapabilities()` and `mcpServerStatus()` against an injected MCP registry port. Topology also requires the package-local MCP capability type and status projection methods to remain on the runtime shell.
- Sixty-third verification-chain increment complete: focused package-local runtime tests now cover `mcpListTools()` projection from package-local MCP capabilities. Topology also requires the tool-list projection method to remain on the runtime shell.
- Sixty-fourth verification-chain increment complete: focused package-local runtime tests now cover `mcpConnect()`, `mcpDisconnect()`, and `mcpReconnect()` action ordering against an injected MCP registry port, including missing configured-server failures. Topology also requires these action methods and refresh/registration port seams to remain on the runtime shell.
- Sixty-fifth verification-chain increment complete: focused package-local runtime tests now cover `filterTools()` for empty allowlists, combined allow/deny lists, and deny-only filtering. Topology also requires the package-local named-tool contract and `allowedTools !== undefined` guard to remain in the runtime shell.
- Sixty-sixth verification-chain increment complete: focused package-local runtime tests now cover `registerTools()` against an injected tool catalog port, including filtered registrations and empty-filter skips. Topology also requires the package-local tool catalog and source contracts to remain on the runtime shell.
- Sixty-seventh verification-chain increment complete: focused package-local runtime tests now cover `registerConfiguredMcpServers()` against injected MCP registry and logger ports, including in-process handles, disabled remote configs, isolated registration warnings, and configured-server refreshes. Topology also requires those registration and logger seams to remain on the runtime shell.
- Sixty-eighth verification-chain increment complete: focused package-local runtime tests now cover MCP tool refresh registration, including per-server removal, available-tool reads, allowlist filtering, MCP source metadata, and catalog registration. Topology also requires the package-local registry/catalog refresh seams and server-name derivation helper to remain on the runtime shell.
- Sixty-ninth verification-chain increment complete: focused package-local runtime tests now cover custom tool registration through an injected factory port, including empty-list skips, configured definition conversion, allowlist filtering, and custom/workspace/session catalog source metadata. Topology also requires the custom tool registration method and factory port to remain on the runtime shell.
- Seventieth verification-chain increment complete: focused package-local runtime tests now cover builtin tool registration through an injected provider port, including provider context, allow/deny filtering, trusted builtin catalog metadata, and empty-filter skips. Topology also requires the builtin registration method and provider context/port contracts to remain on the runtime shell.
- Seventy-first verification-chain increment complete: focused package-local runtime tests now cover subagent initialization through an injected registry port, including logger/project-dir wiring, standard location loading, and session agent definition conversion. Topology also requires the subagent registry port and package-local conversion helper to remain on the runtime shell.
- Seventy-second verification-chain increment complete: focused package-local runtime tests now cover permission handler composition, proving PermissionRequest hooks update input before `canUseTool` sees it while preserving the existing fallback ask result. Topology also requires the permission hook port and package-local permission factory usage to remain on the runtime shell.
- Seventy-third verification-chain increment complete: focused package-local runtime tests now cover hook manager initialization, proving sessions without hooks do not enable hooks while sessions with configured hook callbacks do. Topology also requires the hook manager port and `initializeHooks()` to remain on the runtime shell.
- Seventy-fourth verification-chain increment complete: focused package-local runtime tests now cover execution pipeline assembly through an injected factory port, including permission config defaults, session permission mode propagation, permission handler creation, logger wiring, catalog wiring, and max-history settings. Topology also requires the execution pipeline factory seam to remain on the runtime shell.
- Seventy-fifth verification-chain increment complete: focused package-local runtime tests now cover `getAgentRuntimeDeps()` projection through injected package-local ports, including cached execution pipeline creation, default context forwarding, MCP/subagent/background/hook port forwarding, runtime-managed marking, and logger forwarding. Topology also requires the agent runtime deps contract, hook runtime port, and background agent manager port to remain on the root-import-free package-local runtime shell.
- Seventy-sixth verification-chain increment complete: focused package-local runtime tests now cover kernel port creation through an injected factory, proving tool/store/trace/hook getters forward the cached execution pipeline and SDK-local dependencies into package-local seams. Topology also requires the kernel port factory contract and all four getters to remain on the root-import-free package-local runtime shell.
- Seventy-seventh verification-chain increment complete: focused package-local runtime tests now cover `createAgentKernel()` for both explicit model ports and resolver-backed configured models, proving kernel creation receives model defaults, store/hook/trace/tool ports, max steps, trace context tokens, and execution-context hooks through injected package-local seams. Topology also requires the kernel factory and model resolver contracts to remain on the root-import-free package-local runtime shell.
- Seventy-eighth verification-chain increment complete: focused package-local runtime tests now cover kernel event projection for content, hidden/visible thinking, permission updates, usage metadata, and successful results. Topology also requires the stream projection method, projection options contract, usage helper, and permission-update helper to remain on the root-import-free package-local runtime shell.
- Seventy-ninth verification-chain increment complete: focused package-local runtime tests now cover `streamAgentKernelTurn()` for turn-start emission, runnable kernel invocation, thinking visibility, usage context-token projection, result completion, max-step forwarding, and typed runnable kernel ports. Topology also requires the stream method, stream options contract, and kernel port contract to remain on the root-import-free package-local runtime shell.
- Eightieth verification-chain increment complete: focused package-local tests now cover `createKernelStreamTurnBridge()` for snapshot preparation, multimodal text extraction, turn id/signal/thinking forwarding, max-turn to max-step mapping, execution-context construction, and stream passthrough. Topology also requires the kernel stream bridge source to remain package-local and root-import-free.
- Eighty-first verification-chain increment complete: focused package-local tests now cover `createPackageLocalKernelSessionRuntimeFactory()` for create/resume session construction, kernel runtime context propagation, package-local stream wiring, max-turn forwarding through the bridge, and cleanup forwarding. Topology also requires the kernel runtime factory assembly to remain package-local and root-import-free.
- Eighty-second verification-chain increment complete: focused session factory tests now cover the kernel-first loader seam, proving an injected kernel runtime factory handles create/resume and prevents the legacy fallback from loading. Topology also requires the default runtime factory to keep the kernel loader seam alongside the temporary legacy fallback.
- Eighty-third verification-chain increment complete: focused default-kernel factory tests now cover package-local kernel session assembly from `SessionOptions`, injected kernel/model runtime ports, sampling/default-context forwarding, max-turn to max-step propagation, usage projection, and runtime cleanup. Topology also requires the default kernel runtime factory skeleton to remain package-local and root-import-free.
- Eighty-fourth verification-chain increment complete: focused kernel model resolver tests now cover current-model and explicit-model resolution, provider adapter option mapping, model request defaults, and missing-model errors. Topology also requires the resolver to stay package-local, root-import-free, and wired into the default kernel runtime factory.
- Eighty-fifth verification-chain increment complete: focused kernel factory tests now cover the package-local `AgentKernel` adapter with a real model turn, and default-kernel factory tests prove the default session runtime no longer requires an injected kernel factory. Topology also requires the adapter to stay package-local, root-import-free, imported from `@blade-ai/agent`, and wired into `createDefaultKernelSessionRuntimeFactory()`.
- Eighty-sixth verification-chain increment complete: focused session factory tests now prove the default lifecycle returns package-local kernel sessions without loading the temporary legacy adapter, and topology rejects `legacySessionAdapter` fallback strings from `runtimeFactory.ts` while requiring direct `createDefaultKernelSessionRuntimeFactory()` assembly.
- Eighty-seventh verification-chain increment complete: topology now requires `packages/agent-sdk/src/session/legacySessionAdapter.ts` to be absent, proving the root `src/session/Session.js` dynamic bridge has been retired from the package-local session source. The obsolete adapter unit test was removed with the implementation so verification focuses on the kernel default and remaining package-local seams.
- Eighty-eighth verification-chain increment complete: topology now requires package-local legacy stream helper files and `packageLocalLegacyRuntimeFactory.ts` to be absent, while session docs contract rejects the old `runtime: 'legacy'` and `experimentalKernel` stream options. Focused verification now targets the kernel stream bridge, default session factory, and package-local runtime tests instead of the retired legacy stream test suite.
- Eighty-ninth verification-chain increment complete: package verification now checks the packed `@blade-ai/agent-sdk/session` type surface for retired legacy runtime markers, and topology requires those forbidden markers to remain part of `scripts/verify-packages.mjs`. This moves the kernel-only stream contract from source/docs tests into the publishable tarball gate.
- Ninetieth verification-chain increment complete: the CI workflow now runs the same full `pnpm run verify` production gate used before release, instead of the narrower lint/type-check/build/test subset. Workflow contract tests assert that pushes and pull requests install dependencies and then execute the complete verify chain, covering docs, package boundaries, entrypoints, packed tarballs, unit tests, and integration-test handling before changes can merge.
- Ninety-first verification-chain increment complete: integration tests now reuse the shared GLM live config loader so `.env`, `GLM_*`, and `INTEGRATION_*` aliases resolve through the same path as live smoke tests. Default `test:integration` remains skipped for CI/release stability unless `INTEGRATION_LIVE=1` is set, while `test:integration:live` is the explicit full real-GLM entrypoint.
- Ninety-second verification-chain increment complete: publishable package manifests now carry npm-facing metadata for license, engines, side-effect safety, homepage, bugs, and keywords, each package has its own README, and `pnpm run verify:packages` requires `package/README.md` inside every packed tarball before running the temporary-consumer install smoke.
- Packed package npm metadata gate complete: `pnpm run verify:packages` now extracts each packed tarball and rejects package manifests whose `license`, `homepage`, `bugs`, or `repository` metadata drift from the source release contract before any npm publication can start. This mirrors the post-publish metadata gate while catching metadata loss at the pre-release tarball stage.
- Package description metadata artifact gate complete: source release verification, packed-package manifest checks, and post-publish temporary-consumer manifest checks now reject package descriptions that drift from the three public package roles: provider-agnostic model interfaces, runtime-independent agent kernel contracts, and the session-first SDK facade. The package description metadata artifact gate keeps npm search results and agent-readable package identity aligned with the Pi-style split.
- Package author metadata artifact gate complete: all three publishable package manifests now declare `author: "echoVic"`, and source release verification, packed-package manifest checks, and post-publish temporary-consumer manifest checks reject artifacts whose author metadata drifts. The package author metadata artifact gate keeps npm-visible maintainer identity consistent across source, tarballs, and installed packages.
- Package discoverability metadata artifact gate complete: packed-package and post-publish temporary-consumer manifest checks now reject npm artifacts whose keywords drop the baseline `agent`, `sdk`, or `llm` discoverability markers. The package discoverability metadata artifact gate keeps the npm-visible search contract aligned with the source release verifier after pack, publish, and install.
- Package engine metadata artifact gate complete: packed-package and post-publish temporary-consumer manifest checks now reject npm artifacts whose `engines.node` metadata drifts from `>=22.14.0`. The package engine metadata artifact gate keeps the Node runtime contract visible in both the tarballs semantic-release will publish and the installed packages external users consume.
- Package module metadata artifact gate complete: packed-package and post-publish temporary-consumer manifest checks now reject npm artifacts whose `type` metadata drifts from `module` or whose `sideEffects` metadata stops being `false`. The package module metadata artifact gate keeps the ESM-only and side-effect-free package contract visible in both pre-publication tarballs and installed npm artifacts.
- Package license artifact gate complete: each publishable package now includes its own `LICENSE` file, `pnpm run verify:release` requires `LICENSE` in the publish files whitelist, `pnpm run verify:packages` rejects tarballs missing `package/LICENSE` or the MIT permission grant, and `pnpm run verify:published -- --version <version>` rejects installed npm artifacts missing the same license text. The package license artifact gate makes the npm-visible legal contract explicit instead of relying only on manifest metadata.
- Packed package manifest entry target gate complete: `pnpm run verify:packages` now extracts each packed tarball and rejects package manifest `main`, `types`, and `exports` targets that point outside `./dist/*` or the explicit `./package.json` metadata export. The packed package manifest entry target gate mirrors the post-publish manifest entry target gate while catching source-file, absolute-path, and package-escape regressions before publication.
- Packed SDK browser export condition gate complete: `pnpm run verify:packages` now rejects packed `@blade-ai/agent-sdk` manifests whose root browser condition no longer resolves to `./dist/browser/index.js`, or whose `server`, `session`, and `local` browser conditions no longer resolve to the explicit server-only stub. The packed SDK browser export condition gate catches browser-stub regressions before npm publication, complementing the temporary-consumer browser bundle smoke.
- Ninety-third verification-chain increment complete: `pnpm run verify:packages` now writes a TypeScript temporary-consumer file after installing the packed tarballs and runs `tsc --noEmit` against public `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` imports, proving package declarations support real external ModelPort, AgentKernel, SessionOptions, StreamMessage, defineTool, and ToolKind usage.
- Ninety-fourth verification-chain increment complete: the packed temporary consumer now imports and type-checks the provider-focused `@blade-ai/ai/deepseek`, `@blade-ai/ai/providers/openai-compatible`, and `@blade-ai/ai/providers/vercel` subpaths, while tarball checks require their JS and declaration artifacts so exported provider contracts cannot regress silently.
- Ninety-fifth verification-chain increment complete: `pnpm run verify:packages` now browser-bundles a temporary consumer against the packed `@blade-ai/agent-sdk` tarball with `--platform=browser` and `--conditions=browser`, proving the root browser condition resolves to the server-only `createSession` stub while browser-safe `core` and `tools` contracts stay free of Node-only dependency markers.
- Ninety-sixth verification-chain increment complete: the packed browser-bundle consumer now also imports `@blade-ai/agent-sdk/session`, `@blade-ai/agent-sdk/server`, and `@blade-ai/agent-sdk/local` under browser conditions and executes their stubs, proving server/local/session subpaths fail clearly in browser bundles without dragging Node, MCP, shell, or local runtime dependencies into client output.
- Ninety-seventh verification-chain increment complete: `pnpm run verify:packages` now type-checks packed temporary-consumer imports across the public `@blade-ai/agent-sdk/core`, `session`, `tools`, `local`, `server`, and `browser` subpaths, proving subpath declarations remain usable directly and do not rely on root-only export luck.
- Ninety-eighth verification-chain increment complete: `pnpm run verify:packages` now type-checks packed temporary-consumer imports across the public `@blade-ai/agent/kernel`, `protocol`, `ports`, `state`, and `tracing` subpaths, proving the runtime-independent agent kernel declarations are consumable directly instead of relying on root re-export coverage.
- Ninety-ninth verification-chain increment complete: `pnpm run verify:packages` now type-checks packed temporary-consumer imports across the public `@blade-ai/ai/chat`, `model`, and `retry` subpaths, proving chat compatibility contracts, provider-agnostic model contracts, and retry policy helpers are consumable directly instead of relying on root re-export coverage.
- One-hundredth verification-chain increment complete: `pnpm run verify:entrypoints` now runs its browser bundle smoke through the esbuild JS API instead of an external CLI subprocess, reducing process-wrapper variance while keeping the browser-condition bundle and Node-only marker checks intact.
- One-hundred-first verification-chain increment complete: `pnpm run verify:packages` now writes and executes a packed temporary-consumer `consumer-runtime.mjs` file that imports public runtime subpaths, asserts key value exports for `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk`, and verifies type-only subpaths stay empty at runtime, proving published tarballs expose the intended Node ESM runtime surface rather than merely resolving module specifiers.
- One-hundred-second verification-chain increment complete: `pnpm run verify:packages` now browser-bundles the packed `@blade-ai/agent` root and `kernel` subpath through the esbuild JS API, executes the bundle smoke, and scans it for Node-only markers, proving the runtime-independent agent kernel remains browser-bundle-safe after publish instead of relying only on source and manifest boundary checks.
- One-hundred-third verification-chain increment complete: `pnpm run verify:boundaries` now rejects `@blade-ai/ai` manifest dependencies on `@blade-ai/agent` or `@blade-ai/agent-sdk`, proving provider-layer ownership cannot regress through package metadata before source imports are scanned.
- One-hundred-fourth verification-chain increment complete: `pnpm run verify:boundaries` now rejects `@blade-ai/agent` manifest dependencies on `@blade-ai/agent-sdk`, proving the runtime-independent kernel cannot gain a session-layer dependency through package metadata even before source imports are scanned.
- One-hundred-fifth verification-chain increment complete: all three publishable packages now expose `./package.json` metadata subpaths, and `pnpm run verify:packages` runtime-loads those metadata exports from packed temporary-consumer installs, proving package names and provenance metadata remain consumable after npm packing.
- One-hundred-sixth verification-chain increment complete: `pnpm run verify:boundaries` now rejects direct `ai` provider-runtime dependencies in the `@blade-ai/agent-sdk` manifest, and the publishable session SDK relies on `@blade-ai/ai` for provider execution ownership instead of declaring the provider runtime itself.
- One-hundred-seventh verification-chain increment complete: browser-bundle verification now routes esbuild calls through a shared retry helper that retries once when the esbuild service exits with either `The service was stopped` or `The service is no longer running`, preserving hard failures for unrelated bundle errors while reducing transient JS API service flake in `pnpm run verify`.
- One-hundred-eighth verification-chain increment complete: `pnpm run verify` now includes `pnpm run verify:release`, a token-free release configuration gate that checks semantic-release branch/tag/plugin shape, all three publishable package metadata blocks, the release workflow's verify-before-release command order, npm registry setup, GitHub token wiring, and absence of long-lived `NPM_TOKEN` publishing.
- One-hundred-ninth verification-chain increment complete: topology now requires the private root workspace package to stay an orchestrator without npm `files` or `publishConfig` metadata, keeping the publish surface exclusively on `packages/ai`, `packages/agent`, and `packages/agent-sdk`.
- One-hundred-tenth verification-chain increment complete: topology now requires the private root workspace package to omit npm entrypoint metadata (`main`, `types`, and `exports`), while package entrypoint tests assert the session-first public export surface from `packages/agent-sdk/package.json`, proving the publish contract lives on the publishable SDK package instead of the root orchestrator.
- One-hundred-eleventh verification-chain increment complete: root `build` now delegates only to the publishable `@blade-ai/agent-sdk` package build instead of producing root `dist` output, and `verify:entrypoints` builds that package directly before browser/import smoke checks. The esbuild bundle helper also resets the JS service before each first build attempt so stale service state is cleared before browser-bundle verification.
- One-hundred-twelfth verification-chain increment complete: topology now requires the private root workspace package to omit runtime `dependencies` and `optionalDependencies`, leaving root package dependencies as dev-only workspace/test/tooling inputs while publishable runtime ownership stays in `packages/ai`, `packages/agent`, and `packages/agent-sdk`.
- One-hundred-thirteenth verification-chain increment complete: topology now requires obsolete root package build config files (`tsup.config.ts` and `tsconfig.build.json`) to be absent, keeping build ownership exclusively in the publishable package directories while root `pnpm run build` remains an orchestrator command.
- One-hundred-fourteenth verification-chain increment complete: `pnpm run verify:examples` now type-checks `@blade-ai/agent-sdk` examples against `packages/agent-sdk/src/index.ts` instead of the legacy root `src/index.ts`, proving quickstart examples exercise the publishable package facade during local verification.
- One-hundred-fifteenth verification-chain increment complete: `pnpm run verify:examples` now forbids the legacy `@/* -> src/*` alias and maps public `@blade-ai/agent` plus `@blade-ai/agent-sdk` subpaths to package-local source facades, proving examples cannot silently bypass the monorepo package boundaries during local type-checking.
- One-hundred-sixteenth verification-chain increment complete: the `@blade-ai/agent-sdk` public declaration overlay now resolves `@blade-ai/ai` public imports from built package declarations instead of inherited workspace source aliases, and topology forbids `../../src`, `../ai/src`, and `../agent/src` aliases in that overlay config.
- One-hundred-seventeenth verification-chain increment complete: the `@blade-ai/agent-sdk` package type-check program now includes only package-local `src/**/*` and no longer exposes the legacy `@/* -> ../../src/*` alias, proving package-local SDK source can type-check without treating the root implementation tree as part of the package.
- One-hundred-eighteenth verification-chain increment complete: the `@blade-ai/agent-sdk` declaration build now emits from package-local `src/**/*` instead of root `../../src/**/*`, resolves `@blade-ai/ai` and `@blade-ai/agent` imports from built declarations, and packed package verification forbids representative legacy root declaration directories such as `dist/agent`, `dist/context`, and `dist/mcp`.
- One-hundred-nineteenth verification-chain increment complete: all publishable package declaration builds now disable declaration maps, and `pnpm run verify:packages` dynamically rejects any packed `.d.ts.map` entry so npm tarballs keep their type surface concise and do not ship local source-map metadata.
- One-hundred-twentieth verification-chain increment complete: all publishable package JS builds now disable source maps, and `pnpm run verify:packages` dynamically rejects any packed `.js.map` entry so npm tarballs do not ship local JavaScript source-map metadata.
- One-hundred-twenty-first verification-chain increment complete: topology now guards the remaining root session source against migration-era stream runtime markers, rejecting `runtime?: 'kernel' | 'legacy'`, `experimentalKernel`, and explicit legacy/kernel branch checks in `src/session/types.ts` and `src/session/Session.ts` so the source contract stays aligned with the packed kernel-only session declarations.
- One-hundred-twenty-second verification-chain increment complete: browser-bundle verification now tolerates two consecutive transient esbuild service stops by default, and the post-publish browser bundle smoke reuses the same retry helper while still loading esbuild from the temporary npm consumer install.
- One-hundred-twenty-third verification-chain increment complete: the package-local `Session` instance now owns basic control state for `setPermissionMode()`, `setModel()`, and `setMaxTurns()` without requiring a legacy delegate. Focused tests prove updated model, permission mode, and max-turn defaults flow into later stream context and supported-model projection.
- One-hundred-twenty-fourth verification-chain increment complete: package-local kernel sessions now inject a `PackageLocalSessionRuntimePort` into `PackageLocalSession`, routing MCP status/list/actions through the package-local runtime instead of requiring a legacy delegate. Focused tests prove `mcpConnect()`, `mcpDisconnect()`, and `mcpReconnect()` reach the kernel runtime MCP registry from the session-first API.
- One-hundred-twenty-fifth verification-chain increment complete: `PackageLocalSession.fork()` now routes through the package-local runtime port before falling back to the migration delegate, and missing fork support returns an explicit capability error instead of a migration-era stub. Focused tests prove runtime-injected fork works from both direct session construction and the package-local runtime factory.
- One-hundred-twenty-sixth verification-chain increment complete: the package-local JSONL session store can now materialize a fork snapshot into a new session file via `writeForkState()`, preserving forked message history and parent session metadata in the same event format consumed by `loadState()`. Focused tests prove `forkState()` output can be written and reloaded without using the legacy root session store.
- One-hundred-twenty-seventh verification-chain increment complete: the default package-local kernel runtime now wires `storagePath` to a package-local JSONL session store adapter and implements `fork()` by reading parent history, materializing a new child session file, and returning a new package-local session through the same runtime factory. Focused tests prove a resumed session can fork persisted JSONL history without using the legacy root session runtime.
- One-hundred-twenty-eighth verification-chain increment complete: the default package-local kernel store port now persists kernel-appended turn messages into the package-local JSONL session store instead of dropping them through a noop store. Focused tests prove a session created through the default kernel runtime writes user and assistant messages, kernel turn metadata, and reasoning content into history that `loadState()` can read back.
- One-hundred-twenty-ninth verification-chain increment complete: package-local create/resume lifecycle now runs through the default kernel runtime factory before returning sessions, calling `ensureSessionCreated()` for new sessions and `ensureSessionLoaded()` for resumed sessions. Focused tests prove empty created sessions and missing resumed sessions both materialize `session_created` JSONL state without sending a prompt or importing the legacy root session runtime.
- One-hundred-thirtieth verification-chain increment complete: package-local sessions now hydrate the synchronous `session.messages` view from the JSONL store during create/resume initialization. Focused tests prove a resumed default kernel session exposes previously persisted user and assistant history through the session-first API without falling back to a legacy delegate.
- One-hundred-thirty-first verification-chain increment complete: package-local sessions now refresh the synchronous `session.messages` view after a successfully streamed default-kernel turn. Focused tests prove newly persisted JSONL user and assistant messages become visible on the current session instance immediately after `send()` plus `stream()` without requiring resume or a legacy delegate.
- One-hundred-thirty-second verification-chain increment complete: package-local sessions now read observability traces from the package-local runtime port, and the default kernel runtime owns a `SessionTraceManager` that creates, finalizes, remembers, and sinks traces for streamed kernel turns. Focused tests prove `session.getLastTrace()` and `session.getTraces()` work after `createSession()` plus `send()`/`stream()` without requiring a legacy delegate.
- One-hundred-thirty-third verification-chain increment complete: the default package-local kernel port factory now maps `@blade-ai/agent` trace events into SDK `TraceRecorder` events through a package-local trace adapter instead of a noop trace port. Focused tests prove `createSession()` traces include kernel `turn_start`, `model_request`, `model_response`, `usage`, and `turn_end` events with normalized usage metadata.
- One-hundred-thirty-fourth verification-chain increment complete: `PackageLocalSession` no longer exposes or falls back to a legacy delegate compatibility surface. Focused topology tests now reject `PackageLocalSessionDelegate`, `delegate?:`, and `this.delegate` markers, proving package-local session behavior is owned by local state plus explicit runtime ports.
- One-hundred-thirty-fifth verification-chain increment complete: the package-local session layer no longer keeps the migration-era prompt-submit, stream-completion, or task-completed helper modules. Topology now requires `promptSubmit.ts`, `streamCompletion.ts`, and `taskCompleted.ts` to be absent, proving the remaining package-local session path is owned by the kernel runtime bridge, session runtime port, trace manager, and message store instead of retired legacy stream fragments.
- One-hundred-thirty-sixth verification-chain increment complete: package-local kernel stream projection now lives in `kernelStreamProjection.ts` instead of as methods and helper functions on `PackageLocalSessionRuntime`. Focused projection tests prove content, thinking visibility, tool permission updates, usage normalization, and result messages are converted without runtime state, while topology rejects reintroducing the usage and permission-update mapper helpers into the runtime class.
- One-hundred-thirty-seventh verification-chain increment complete: package-local runtime noop port implementations now live in `runtimeNoopPorts.ts` instead of at the bottom of `PackageLocalSessionRuntime`. Focused noop-port tests prove inert session store, MCP registry, kernel tool port, kernel factory, and kernel model resolver behavior, while topology rejects reintroducing `createNoopRuntime*` helper functions into the runtime class.
- One-hundred-thirty-eighth verification-chain increment complete: package-local MCP tool source-id mapping now lives in `runtimeMcpTools.ts` instead of as a helper function on `PackageLocalSessionRuntime`. Focused MCP helper tests prove tag-first, legacy `mcp__server__tool` name, and default `mcp` fallback mapping, while topology rejects reintroducing `packageLocalServerNameFromTool()` into the runtime class.
- One-hundred-thirty-ninth verification-chain increment complete: package-local session agent definition mapping now lives in `runtimeSubagents.ts` instead of on `PackageLocalSessionRuntime`. Focused subagent helper tests prove session-defined agents map to registry configs with fallback names, inherited models, explicit models, allowed tools, and `source: "session"`, while topology rejects reintroducing the mapper function into the runtime class.
- One-hundred-fortieth verification-chain increment complete: package-local runtime context helpers now live in `runtimeContext.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove storage-root normalization and project cwd derivation with filesystem capability precedence over environment fallback, while topology rejects reintroducing the local `getRuntimeContextCwd()` helper into the runtime class.
- One-hundred-forty-first verification-chain increment complete: package-local MCP server handle detection now lives in `runtimeMcpServers.ts` instead of on `PackageLocalSessionRuntime`. Focused MCP server helper tests prove in-process handles are recognized without runtime state while remote server configs and incomplete objects are rejected, and topology rejects reintroducing the helper function into the runtime class.
- One-hundred-forty-second verification-chain increment complete: package-local tool filtering policy now lives in `runtimeToolFilters.ts` instead of on `PackageLocalSessionRuntime`. Focused tool-filter tests prove unrestricted, empty allowlist, allowlist-plus-denylist, and denylist-only behavior without runtime state, while topology rejects reintroducing the `allowedTools !== undefined` policy branch into the runtime class.
- One-hundred-forty-third verification-chain increment complete: package-local MCP registry action dispatch now lives in `runtimeMcpServers.ts` instead of private methods on `PackageLocalSessionRuntime`. Focused MCP server helper tests prove connect/disconnect-style actions and in-process/remote registration keep registry `this` binding and emit controlled missing-capability errors, while topology rejects reintroducing the private registry action methods into the runtime class.
- One-hundred-forty-fourth verification-chain increment complete: package-local permission handler composition now lives in `runtimePermissions.ts` instead of on `PackageLocalSessionRuntime`. Focused permission helper tests prove no-policy undefined behavior, permission hook input updates before base handlers, hook decision short-circuiting, and the default ask fallback, while topology rejects reintroducing composite permission construction into the runtime class.
- One-hundred-forty-fifth verification-chain increment complete: package-local kernel model resolution now lives in `runtimeKernelModels.ts` instead of on `PackageLocalSessionRuntime`. Focused kernel-model helper tests prove injected models bypass the resolver while configured model ids still resolve through the resolver with `bladeConfig`, and topology rejects reintroducing the private `resolveAgentKernelModel()` method into the runtime class.
- One-hundred-forty-sixth verification-chain increment complete: package-local hook initialization now lives in `runtimeHooks.ts` instead of on `PackageLocalSessionRuntime`. Focused hook helper tests prove empty hook configuration leaves the manager disabled while configured hook callbacks enable it once, and topology rejects reintroducing direct `hookManager.enable()` activation logic into the runtime class.
- One-hundred-forty-seventh verification-chain increment complete: package-local execution pipeline assembly now lives in `runtimeExecutionPipeline.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove permission defaults, configured permission lists, permission mode fallback, handler forwarding, logger/catalog seams, and factory invocation without runtime state, while topology rejects reintroducing permission-config and factory-create assembly into the runtime class.
- One-hundred-forty-eighth verification-chain increment complete: package-local agent kernel assembly now lives in `runtimeAgentKernels.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove minimal model/store/hook wiring plus optional model defaults, trace, tools, and max-step forwarding, while topology rejects reintroducing direct `kernelFactory.create()` option assembly into the runtime class.
- One-hundred-forty-ninth verification-chain increment complete: package-local trace collector stream scoping now lives in `runtimeHooks.ts` instead of on `PackageLocalSessionRuntime`. Focused hook helper tests prove collectors are set before streaming and cleared after both successful and failing async streams, while topology rejects reintroducing direct `hookRuntime.setTraceCollector()` calls into the runtime class.
- One-hundred-fiftieth verification-chain increment complete: package-local kernel trace finalization now lives in `runtimeKernelTraceFinalization.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove usage is retained for successful result finalization, kernel error events finalize with message/code, and thrown stream errors finalize without being swallowed, while topology rejects reintroducing trace-finalizer branches into the runtime class.
- One-hundred-fifty-first verification-chain increment complete: package-local kernel turn stream orchestration now lives in `runtimeKernelTurnStream.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove kernel event projection, trace collector scoping, successful trace finalization with retained usage, thrown-error finalization, and run-turn option forwarding, while topology rejects reintroducing direct kernel stream loops and runtimeInstance type imports into the runtime class and bridge.
- One-hundred-fifty-second verification-chain increment complete: package-local kernel turn resolution now lives in `runtimeKernelTurnStream.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove stream turns resolve configured kernel models, create trace recorders, pass trace-aware kernel options into kernel construction, preserve max-context usage projection, and finalize successful traces, while topology rejects reintroducing local trace-recorder/kernel/max-context assembly into the runtime class.
- One-hundred-fifty-third verification-chain increment complete: package-local runtime trace manager initialization now lives in `runtimeTraceManager.ts` instead of inside `PackageLocalSessionRuntime` construction. Focused helper tests prove trace metadata captures model, provider, and default permission mode, and sink failures are routed through the runtime logger, while topology rejects reintroducing inline `SessionTraceManager` construction and trace-sink logging in the runtime class.
- One-hundred-fifty-fourth verification-chain increment complete: package-local MCP capability projection now lives in `runtimeMcpCapabilities.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove server capabilities project into public MCP server status and tool-list shapes, while topology rejects reintroducing inline capability `map`/`flatMap` projections in the runtime class.
- One-hundred-fifty-fifth verification-chain increment complete: package-local MCP tool refresh orchestration now lives in `runtimeMcpTools.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove registry tool lookup, per-server catalog cleanup, filtering, and remote MCP source registration without runtime state, while topology rejects reintroducing inline MCP tool removal and registration in the runtime class.
- One-hundred-fifty-sixth verification-chain increment complete: package-local configured MCP server registration now lives in `runtimeMcpServers.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove in-process registration, disabled remote-server skipping, remote registration failure warnings, and post-registration tool refresh without runtime state, while topology rejects reintroducing inline configured-server iteration and disabled checks in the runtime class.
- One-hundred-fifty-seventh verification-chain increment complete: package-local MCP ensure-registration now lives in `runtimeMcpServers.ts` instead of a private `PackageLocalSessionRuntime` method. Focused helper tests prove missing configured servers produce controlled errors and existing servers delegate through the registry with binding preserved, while topology rejects reintroducing the private runtime ensure method.
- One-hundred-fifty-eighth verification-chain increment complete: package-local custom tool registration now lives in `runtimeToolRegistration.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove empty definitions are skipped, missing factories produce controlled errors, and configured tools are mapped through the custom tool factory into the standard workspace registration source, while topology rejects reintroducing inline custom tool mapping and factory errors in the runtime class.
- One-hundred-fifty-ninth verification-chain increment complete: package-local builtin tool registration now lives in `runtimeToolRegistration.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove builtin providers receive the session id, storage root, MCP registry, and protocol-tool exclusion context before registering trusted builtin sources, while topology rejects reintroducing inline builtin provider context construction in the runtime class.
- One-hundred-sixtieth verification-chain increment complete: package-local subagent runtime initialization now lives in `runtimeSubagents.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove logger/project setup, standard-location loading, and session-defined subagent registration with override semantics without runtime state, while topology rejects reintroducing inline subagent registry setup and agent iteration in the runtime class.
- One-hundred-sixty-first verification-chain increment complete: package-local runtime forking now lives in `runtimeForking.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove fork state materialization, fork session id creation, child session creation, and controlled missing-capability/not-found/materialization errors without runtime state, while topology rejects reintroducing inline fork orchestration in the runtime class.
- One-hundred-sixty-second verification-chain increment complete: package-local MCP server lifecycle orchestration now lives in `runtimeMcpServers.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove connect, disconnect, and reconnect preserve registry binding, ensure configured servers before connect/reconnect, and refresh MCP tools after each lifecycle action without runtime state, while topology rejects reintroducing inline registry-action calls in the runtime class.
- One-hundred-sixty-third verification-chain increment complete: package-local execution pipeline caching now lives in `runtimeExecutionPipeline.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove cached pipeline creation happens once outside the runtime class, while topology rejects reintroducing runtime-owned execution pipeline cache fields.
- One-hundred-sixty-fourth verification-chain increment complete: package-local kernel port assembly now lives in `runtimeKernelPorts.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove tool, store, trace, and hook ports are created through the injected kernel port factory with typed port inputs and without runtime state, while topology rejects reintroducing inline `kernelPortFactory.create*` option assembly in the runtime class.
- One-hundred-sixty-fifth verification-chain increment complete: package-local agent runtime dependency projection now lives in `runtimeAgentDeps.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove execution pipeline, context, MCP registry, subagent registry, background manager, hook runtime, logger, and the `runtimeManaged` marker are projected without runtime state, while topology rejects reintroducing inline dependency object assembly in the runtime class.
- One-hundred-sixty-sixth verification-chain increment complete: package-local agent kernel port projection now lives in `runtimeAgentKernels.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove required store/hook ports plus optional trace/tool ports are projected from resolved kernel model defaults without runtime state, while topology rejects reintroducing inline store/hooks/trace/tools port object assembly in the runtime class.
- One-hundred-sixty-seventh verification-chain increment complete: package-local resolved agent-kernel orchestration now lives in `runtimeAgentKernels.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove resolved kernel models are combined with projected store/hooks/trace/tools ports and max-step options without runtime state, while topology rejects reintroducing direct port projection or low-level kernel creation calls in the runtime class.
- One-hundred-sixty-eighth verification-chain increment complete: package-local options-level agent-kernel orchestration now lives in `runtimeAgentKernels.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove model ids are resolved through the injected kernel model resolver before projected trace/tool/store/hook ports and max-step options are assembled, while topology rejects reintroducing direct `resolvePackageLocalRuntimeKernelModel` calls in the runtime class.
- One-hundred-sixty-ninth verification-chain increment complete: package-local resolved agent-kernel creator construction now lives in `runtimeAgentKernels.ts` instead of on `PackageLocalSessionRuntime`. Focused helper tests prove stream-runtime resolved kernel callbacks can be created from injected kernel factory and port accessors without runtime state, while topology rejects reintroducing the private `createAgentKernelFromResolved()` method or direct resolved-kernel helper calls in the runtime class.
- One-hundred-seventieth verification-chain increment complete: package-local agent-kernel operations bundling now lives in `runtimeAgentKernels.ts` instead of repeated wiring inside `PackageLocalSessionRuntime`. Focused helper tests prove options-level and resolved kernel creation share the same injected kernel factory and port accessors without runtime state, while topology rejects reintroducing direct options/resolved creator calls in the runtime class.
- One-hundred-seventy-first verification-chain increment complete: package-local kernel-port operations bundling now lives in `runtimeKernelPorts.ts` instead of repeated wiring inside `PackageLocalSessionRuntime`. Focused helper tests prove tool/store/trace/hook port creation share the injected kernel port factory, session store, hook runtime, and lazy execution pipeline access without runtime state, while topology rejects reintroducing direct low-level kernel port helper calls in the runtime class.
- One-hundred-seventy-second verification-chain increment complete: package-local agent runtime dependency operations now live in `runtimeAgentDeps.ts` instead of repeated dependency object assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove execution pipeline creation stays lazy while default context, MCP registry, subagent registry, background manager, hook runtime, and logger are projected without runtime state, while topology rejects reintroducing direct dependency object assembly in the runtime class.
- One-hundred-seventy-third verification-chain increment complete: package-local tool registration operations now live in `runtimeToolRegistration.ts` instead of keeping filtered registration branching inside `PackageLocalSessionRuntime`. Focused helper tests prove the injected filter policy is applied before catalog registration and empty filtered results are skipped without runtime state, while topology rejects reintroducing direct filtered-tool branching or catalog registration in the runtime class.
- One-hundred-seventy-fourth verification-chain increment complete: package-local session lifecycle operations now live in `runtimeSessionLifecycle.ts` instead of direct store create/load/message calls inside `PackageLocalSessionRuntime`. Focused helper tests prove create, load-with-fallback materialization, and message loading are driven by injected store ports without runtime state, while topology rejects reintroducing direct lifecycle store branching in the runtime class.
- One-hundred-seventy-fifth verification-chain increment complete: package-local workspace turn preparation now lives in `runtimeWorkspace.ts` instead of inline workspace update assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove turn cwd overrides stale environment cwd while cwd-less turns preserve the provided environment without runtime state, while topology rejects reintroducing direct workspace update assembly in the runtime class.
- One-hundred-seventy-sixth verification-chain increment complete: package-local MCP capability operations now live in `runtimeMcpCapabilities.ts` instead of direct capability registry and projection calls inside `PackageLocalSessionRuntime`. Focused helper tests prove registry capability reads, server status projection, and public tool listing are bundled behind injected registry ports without runtime state, while topology rejects reintroducing direct MCP capability registry/projection calls in the runtime class.
- One-hundred-seventy-seventh verification-chain increment complete: package-local MCP close lifecycle now lives in `runtimeMcpServers.ts` instead of direct registry shutdown inside `PackageLocalSessionRuntime`. Focused helper tests prove close preserves the injected registry binding while disconnecting all MCP servers without runtime state, while topology rejects reintroducing direct `disconnectAll()` calls in the runtime class.
- One-hundred-seventy-eighth verification-chain increment complete: package-local trace access operations now live in `runtimeTraceManager.ts` instead of direct trace-manager reads inside `PackageLocalSessionRuntime`. Focused helper tests prove last-trace and trace-list reads are projected through an injected trace manager without runtime state, while topology rejects reintroducing direct trace-manager read calls in the runtime class.
- One-hundred-seventy-ninth verification-chain increment complete: package-local MCP server lifecycle operations now live in `runtimeMcpServers.ts` instead of repeated connect/disconnect/reconnect option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove configured-server lookup, registry lifecycle calls, refresh callbacks, and registry binding are preserved behind injected ports without runtime state, while topology rejects reintroducing direct lifecycle helper calls in the runtime class.
- One-hundred-eightieth verification-chain increment complete: package-local MCP server registration operations now live in `runtimeMcpServers.ts` instead of repeated configured-server registration option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove in-process and remote server registration, refresh callbacks, and registry binding are preserved behind injected ports without runtime state, while topology rejects reintroducing direct configured-registration helper calls in the runtime class.
- One-hundred-eighty-first verification-chain increment complete: package-local MCP tool refresh operations now live in `runtimeMcpTools.ts` instead of repeated MCP registry/catalog/filter option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove stale MCP tool removal, available-tool reads, allow/deny filtering, source metadata projection, and catalog registration are preserved behind injected ports without runtime state, while topology rejects reintroducing direct refresh helper calls in the runtime class.
- One-hundred-eighty-second verification-chain increment complete: package-local session tool registration operations now live in `runtimeToolRegistration.ts` instead of repeated custom and builtin tool registration option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove custom factory projection, builtin provider context construction, source metadata, and registration callbacks are preserved behind injected ports without runtime state, while topology rejects reintroducing direct custom/builtin helper calls in the runtime class.
- One-hundred-eighty-third verification-chain increment complete: package-local permission operations now live in `runtimePermissions.ts` instead of permission hook detection and handler option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove `PermissionRequest` hook detection, hook-driven input updates, base handler composition, and fallback ask behavior are preserved behind injected ports without runtime state, while topology rejects reintroducing direct permission helper calls or hook-event branching in the runtime class.
- One-hundred-eighty-fourth verification-chain increment complete: package-local hook initialization operations now live in `runtimeHooks.ts` instead of direct hook-manager initialization calls inside `PackageLocalSessionRuntime`. Focused helper tests prove sessions with configured hooks enable the injected hook manager while empty hook sets stay inert, and topology rejects reintroducing direct hook initialization helper calls in the runtime class.
- One-hundred-eighty-fifth verification-chain increment complete: package-local MCP server close now shares the `runtimeMcpServers.ts` lifecycle operations object with connect, disconnect, and reconnect instead of direct close-helper calls inside `PackageLocalSessionRuntime`. Focused helper tests prove close preserves the injected registry binding while disconnecting all MCP servers, and topology rejects reintroducing direct close-helper option assembly in the runtime class.
- One-hundred-eighty-sixth verification-chain increment complete: package-local subagent initialization operations now live in `runtimeSubagents.ts` instead of direct registry/logger/project/storage/agent option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove logger setup, project-directory setup, standard-location loading, session-agent projection, and override registration are preserved behind injected ports without runtime state, while topology rejects reintroducing direct subagent initialization helper calls in the runtime class.
- One-hundred-eighty-seventh verification-chain increment complete: package-local tool filter operations now live in `runtimeToolFilters.ts` instead of allow/deny option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove explicit empty allowlists still disable all tools, allow/deny precedence is preserved, and unrestricted sessions keep all tools behind injected policy options without runtime state, while topology rejects reintroducing direct filter-helper calls in the runtime class.
- One-hundred-eighty-eighth verification-chain increment complete: package-local kernel turn stream operations now live in `runtimeKernelTurnStream.ts` instead of trace manager, hook runtime, kernel model resolver, and resolved-kernel creator assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove stream operations create default trace recorders, resolve kernel models, create agent kernels, and project stream messages through injected ports without runtime state, while topology rejects reintroducing direct runtime stream-helper calls in the runtime class.
- One-hundred-eighty-ninth verification-chain increment complete: package-local trace runtime bundling now lives in `runtimeTraceManager.ts` instead of separate trace-manager and trace-access operation assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove the bundled trace manager and access operations share the same trace store, preserve model/provider/permission metadata, and remain reusable without runtime state, while topology rejects reintroducing direct low-level trace factory calls in the runtime class.
- One-hundred-ninetieth verification-chain increment complete: package-local runtime port resolution now lives in `runtimeNoopPorts.ts` instead of repeated optional-port fallback assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove injected ports are preserved, noop fallbacks remain inert, and hook manager resolution falls back to the resolved hook runtime without runtime state, while topology rejects reintroducing direct `noopPorts` fallback wiring in the runtime class.
- One-hundred-ninety-first verification-chain increment complete: package-local fork operations now live in `runtimeForking.ts` instead of fork callback and session-store option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove fork operations preserve fork-state materialization, child session id creation, snapshot writes, and child session creation through injected callbacks without runtime state, while topology rejects reintroducing direct fork-helper calls in the runtime class.
- One-hundred-ninety-second verification-chain increment complete: package-local workspace turn operations now live in `runtimeWorkspace.ts` instead of workspace port and snapshot option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove turn-scoped cwd and environment projection still run through injected workspace ports without runtime state, while topology rejects reintroducing direct workspace helper calls in the runtime class.
- One-hundred-ninety-third verification-chain increment complete: package-local execution pipeline operations now live in `runtimeExecutionPipeline.ts` instead of pipeline cache and permission-handler option assembly inside `PackageLocalSessionRuntime`. Focused helper tests prove pipeline creation remains lazy and cached, permission handlers are resolved once at creation time, and factory options preserve permission defaults without runtime state, while topology rejects reintroducing direct pipeline/cache helper calls in the runtime class.
- One-hundred-ninety-fourth verification-chain increment complete: package-local MCP server config operations now live in `runtimeMcpServers.ts` instead of direct `SessionOptions.mcpServers` fallback reads inside `PackageLocalSessionRuntime`. Focused helper tests prove configured server maps are preserved, missing config resolves to an empty map without runtime state, and topology rejects reintroducing direct `this.options.mcpServers ?? {}` reads in the runtime class.
- One-hundred-ninety-fifth verification-chain increment complete: package-local MCP server config, registration, and lifecycle operations are now assembled through a single `createPackageLocalRuntimeMcpServerOperations()` bundle in `runtimeMcpServers.ts`. Focused helper tests prove the bundle shares configured server maps across config/registration/lifecycle paths through injected ports, while topology rejects reintroducing separate MCP server operation factory calls inside `PackageLocalSessionRuntime`.
- One-hundred-ninety-sixth verification-chain increment complete: package-local tool filtering, catalog registration, and session custom/builtin registration are now assembled through a single `createPackageLocalRuntimeToolOperations()` adapter in `runtimeTools.ts`. Focused helper tests prove allow/deny policy, catalog registration, and session tool registration share injected ports without runtime state, while topology rejects reintroducing separate tool filter and registration factory calls inside `PackageLocalSessionRuntime`.
- One-hundred-ninety-seventh verification-chain increment complete: package-local hook initialization and permission handler creation are now assembled through a single `createPackageLocalRuntimeGuardOperations()` adapter in `runtimeGuards.ts`. Focused helper tests prove hook-manager enablement and permission hook input mutation share injected session hook configuration without runtime state, while topology rejects reintroducing separate permission and hook operation factory calls inside `PackageLocalSessionRuntime`.
- One-hundred-ninety-eighth verification-chain increment complete: package-local MCP capability projection, server config/registration/lifecycle, and MCP tool refresh are now assembled through a single `createPackageLocalRuntimeMcpOperations()` adapter in `runtimeMcp.ts`. Focused helper tests prove capability status projection, configured server registration/connection, and filtered MCP tool refresh share injected registry/catalog ports without runtime state, while topology rejects reintroducing separate MCP capability, server, and tool-refresh factory calls inside `PackageLocalSessionRuntime`.
- One-hundred-ninety-ninth verification-chain increment complete: package-local session lifecycle and workspace turn preparation are now assembled through a single `createPackageLocalRuntimeSessionOperations()` adapter in `runtimeSessionOperations.ts`. Focused helper tests prove session create/load/message operations and turn-scoped workspace updates share injected store/workspace ports without runtime state, while topology rejects reintroducing separate lifecycle and workspace operation factory calls inside `PackageLocalSessionRuntime`.
- Two-hundredth verification-chain increment complete: package-local execution pipeline creation and agent runtime dependency projection are now assembled through a single `createPackageLocalRuntimeExecutionOperations()` adapter in `runtimeExecution.ts`. Focused helper tests prove lazy pipeline caching and runtime-managed dependency projection share injected execution/dependency ports without runtime state, while topology rejects reintroducing separate execution-pipeline and agent-deps operation factory calls inside `PackageLocalSessionRuntime`.
- Two-hundred-first verification-chain increment complete: package-local kernel port creation and agent kernel creation are now assembled through a single `createPackageLocalRuntimeKernelOperations()` adapter in `runtimeKernel.ts`. Focused helper tests prove kernel port projection and agent kernel creation share injected port/kernel factories without runtime state, while topology rejects reintroducing separate kernel-port and agent-kernel operation factory calls inside `PackageLocalSessionRuntime`.
- Two-hundred-second verification-chain increment complete: package-local trace runtime creation and kernel turn stream creation are now assembled through a single `createPackageLocalRuntimeTurnOperations()` adapter in `runtimeTurn.ts`. Focused helper tests prove trace access operations and kernel turn streaming share the same trace manager without runtime state, while topology rejects reintroducing separate trace-runtime and kernel-turn-stream operation factory calls inside `PackageLocalSessionRuntime`.
- Two-hundred-third verification-chain increment complete: package-local subagent initialization and session forking are now assembled through a single `createPackageLocalRuntimeSessionCapabilityOperations()` adapter in `runtimeSessionCapabilities.ts`. Focused helper tests prove session-defined agents and fork materialization share injected session options without runtime state, while topology rejects reintroducing separate subagent and fork operation factory calls inside `PackageLocalSessionRuntime`.
- Two-hundred-fourth verification-chain increment complete: `pnpm run verify:boundaries` now treats `packages/agent-sdk/src` as an owned package source tree and rejects relative imports that leave it. Focused verifier tests prove an agent-sdk source file cannot import the legacy root `src` tree through a relative path, moving this package-local ownership rule from topology coverage into the production boundary gate.
- Two-hundred-fifth verification-chain increment complete: `pnpm run verify:boundaries` now also rejects `packages/agent-sdk/src` imports from the package's own public `@blade-ai/agent-sdk` facade or subpaths. Focused verifier tests prove package-local implementation files cannot route through the publishable SDK entrypoint, preventing circular facade dependencies while keeping public consumer import smoke tests in the package verifier.
- Two-hundred-sixth verification-chain increment complete: `pnpm run verify:boundaries` now rejects self-imports from all package-local source trees, not only the session SDK. Focused verifier tests prove `packages/ai/src` cannot import `@blade-ai/ai` subpaths and `packages/agent/src` cannot import `@blade-ai/agent` subpaths, preserving Pi-style package source ownership while still allowing the valid `@blade-ai/agent` to `@blade-ai/ai` dependency direction.
- Two-hundred-seventh verification-chain increment complete: `pnpm run verify:boundaries` now validates package build entry ownership in each `tsup.config.ts`. Focused verifier tests prove a package build entry cannot point outside its own `packages/*/src` tree, preventing publishable package builds from silently reaching back into the legacy root source tree after source ownership has been migrated.
- Two-hundred-eighth verification-chain increment complete: `pnpm run verify:boundaries` now validates publish manifest target ownership. Focused verifier tests prove package `main`, `types`, and nested `exports` targets cannot expose source files and must point at `./dist/*` artifacts, with only the explicit `./package.json` export allowed outside dist.
- Two-hundred-ninth verification-chain increment complete: the package-local default kernel runtime now runs callback-based `SessionStart` hooks during session create/resume initialization. Focused default-runtime tests prove `createSession()` invokes configured `SessionStart` callbacks with the package-local session id, resume flag, model, and provider metadata before the session is returned.
- Two-hundred-tenth verification-chain increment complete: the package-local default kernel runtime now runs callback-based `SessionEnd` hooks during session close. Focused default-runtime tests prove closing a package-local session invokes configured `SessionEnd` callbacks with the package-local session id and close reason while runtime cleanup remains owned by the package-local runtime.
- Two-hundred-eleventh verification-chain increment complete: package-local kernel streaming now reports successful kernel results through callback-based `TaskCompleted` hooks. Focused kernel-turn tests prove result events invoke configured task-completion callbacks with the turn id, original task description, result summary, success flag, and abort signal before the success stream result is emitted.
- Two-hundred-twelfth verification-chain increment complete: package-local kernel streaming now reports kernel error events through callback-based `TaskCompleted` hooks. Focused kernel-turn tests prove error events invoke configured task-completion callbacks with the turn id, original task description, error summary, failed success flag, and abort signal while still emitting the session error stream message.
- Two-hundred-thirteenth verification-chain increment complete: package-local kernel streaming now reports thrown stream failures through callback-based `TaskCompleted` hooks before rethrowing. Focused kernel-turn tests prove thrown errors still finalize traces and propagate to callers while invoking task-completion callbacks with the turn id, original task description, error summary, failed success flag, and abort signal.
- Two-hundred-fourteenth verification-chain increment complete: thrown stream failure `TaskCompleted` hooks now run inside the package-local trace collector scope. Focused kernel-turn tests prove the trace recorder is active while failure callbacks run and cleared again before the original stream error is rethrown.
- Two-hundred-fifteenth verification-chain increment complete: thrown stream failure `TaskCompleted` hook failures no longer mask the original stream error or skip trace finalization. Focused kernel-turn tests prove a failing completion hook still runs inside the trace collector scope, the collector is cleared, the trace is finalized with the original stream error, sinks are notified, and callers receive the original stream failure.
- Two-hundred-sixteenth verification-chain increment complete: result and kernel-error `TaskCompleted` hook failures no longer mask completed kernel events or skip trace finalization. Focused kernel-turn tests prove successful result events and kernel error events still project to session stream messages, finalize traces, remember traces, and notify sinks when completion hooks fail.
- Two-hundred-seventeenth verification-chain increment complete: suppressed `TaskCompleted` hook failures are now recorded on the active trace before the kernel stream continues. Focused kernel-turn and trace-recorder tests prove custom hook runtime failures surface as `hook_error` trace events with `suppressed: true` while valid result events still finalize successfully.
- Two-hundred-eighteenth verification-chain increment complete: suppressed `TaskCompleted` hook trace recording is now best-effort and cannot replace valid stream outcomes. Focused kernel-turn tests prove a trace event write failure while recording a suppressed completion hook failure still preserves the successful result stream, success trace finalization, trace retention, and sink notification.
- Two-hundred-nineteenth verification-chain increment complete: session trace finalization is now best-effort when recorder finishing or trace retention fails. Focused trace manager and kernel trace tests prove recorder finish failures resolve as no-ops without calling trace retention or sink notification, keeping observability failures isolated from session turn lifecycles.
- Two-hundred-twentieth verification-chain increment complete: `pnpm run verify:packages` now rejects packed `@blade-ai/agent-sdk/core` declarations that expose server-only session APIs such as `createSession`, `resumeSession`, `forkSession`, or Node-local APIs such as `getBuiltinTools` and `createSdkMcpServer`, keeping the browser-safe core type surface aligned with the Pi-style package boundary.
- Two-hundred-twenty-first verification-chain increment complete: `pnpm run test:live:session-glm` now validates observability on the real session-first GLM path. After a successful `send()` + `stream()` turn with `allowedTools: []`, the script reads `session.getLastTrace()` and requires a successful trace with `model_request`, `turn_end`, and `result` events, plus `usage` when the provider emits stream usage.
- Two-hundred-twenty-second verification-chain increment complete: the root `@blade-ai/agent-sdk` entry now keeps Node-local adapters behind the explicit `@blade-ai/agent-sdk/local` subpath. Focused root export tests prove `getBuiltinTools`, `createSdkMcpServer`, `FileSystemMemoryStore`, and sandbox adapters are absent from the root namespace, while packed and post-publish consumer verifiers reject those symbols from root runtime exports and root declarations.
- Two-hundred-twenty-third verification-chain increment complete: the root `@blade-ai/agent-sdk` entry now also keeps provider-specific DeepSeek helpers inside `@blade-ai/ai/deepseek`. Focused root export tests prove `normalizeDeepSeekModel`, `calculateDeepSeekCost`, `DeepSeekCostTracker`, and `DEEPSEEK_DEFAULT_MODEL` are absent from the SDK root namespace, while packed and post-publish consumer verifiers reject those symbols from root runtime exports and root declarations.
- Two-hundred-twenty-fourth verification-chain increment complete: the `@blade-ai/agent-sdk/server` entry is now an explicit package-local facade over session, core, tools, and subagent exports instead of wildcard-forwarding through the SDK root. Focused entrypoint tests reject `export * from '../index.js'`, and packed plus post-publish verifiers reject server JS or declaration artifacts that point back at the root facade.
- Two-hundred-twenty-fifth verification-chain increment complete: the package-local root and server facades now keep their server-safe runtime values aligned, including `subagentRegistry`, and packed plus post-publish temporary consumers type-check the matching server subpath public surface for `PermissionsConfig`, `ClaudeCodePermissionMode`, `SubagentExecutionRunner`, and `SubagentFrontmatter`.
- Two-hundred-twenty-sixth verification-chain increment complete: packed and post-publish runtime smoke tests now compare the full runtime export key set of `@blade-ai/agent-sdk` and `@blade-ai/agent-sdk/server`, so future server-safe root value exports cannot silently miss the explicit server facade while root-local and provider-helper exclusion checks remain in place.
- Two-hundred-twenty-seventh verification-chain increment complete: packed and post-publish temporary consumers now read the installed `@blade-ai/agent-sdk/dist/index.d.ts` and `@blade-ai/agent-sdk/dist/server/index.d.ts` declaration files and compare their named export sets, so server-safe root type exports cannot silently drift away from the explicit server facade after package build, pack, or publication.
- Two-hundred-twenty-eighth verification-chain increment complete: `pnpm run verify:boundaries` now rejects CLI product entrypoints in the `@blade-ai/agent-sdk` manifest, including a `bin` field or `./cli` export. This keeps the session-first SDK as a server/library facade and reserves future Pi-style coding-agent or CLI product behavior for a separate package.
- Two-hundred-twenty-ninth verification-chain increment complete: packed-package and post-publish temporary-consumer manifest checks now also reject a `bin` field or `./cli` export in the npm-visible `@blade-ai/agent-sdk` manifest, extending the Pi-style CLI product boundary from source metadata to the actual tarball and installed publication artifacts.
- Two-hundred-thirtieth verification-chain increment complete: packed-package and post-publish file-scope checks now reject `dist/cli/*` files in the npm-visible `@blade-ai/agent-sdk` artifact, so CLI product code cannot silently ship inside the session-first SDK tarball even without a manifest `bin` or `./cli` entrypoint.
- Two-hundred-thirty-first documentation increment complete: README, package README, API reference, and production checklist now distinguish CLI process embedding from a CLI product package. The docs explicitly say `@blade-ai/agent-sdk` does not publish a CLI product or `@blade-ai/agent-sdk/cli`, preserving the Pi-style separation between the session-first SDK facade and any future coding-agent / CLI product package.
- Two-hundred-thirty-second verification-chain increment complete: source, packed-package, and post-publish manifest gates now reject the `cli` keyword in `@blade-ai/agent-sdk` metadata. The package keeps npm discoverability focused on the session-first server SDK while still documenting CLI process embedding separately from any future Pi-style CLI product package.
- Two-hundred-thirty-third verification-chain increment complete: `pnpm run verify:boundaries` now applies the CLI product boundary to all three publishable library packages. Focused verifier tests prove `@blade-ai/ai` and `@blade-ai/agent` cannot grow `bin`, `./cli`, or `cli` keyword metadata, keeping Pi-style coding-agent behavior reserved for a future separate product package instead of leaking into model or kernel libraries.
- Two-hundred-thirty-fourth verification-chain increment complete: packed-package and post-publish artifact gates now apply the CLI product boundary to all three publishable library packages. Focused release verifier tests prove `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` installed or packed artifacts cannot expose `bin`, `./cli`, `cli` keyword metadata, or `dist/cli/*` files, keeping source and npm-visible boundaries aligned.
- Two-hundred-thirty-fifth verification-chain increment complete: the package artifact size budget gate now covers packed-package and post-publish artifact checks. `pnpm run verify:packages` rejects oversized packed tarballs before publication, and `pnpm run verify:published -- --version <version>` rejects npm-installed package directories that exceed their published package size budgets, catching accidental source/map/asset bloat beyond the existing file-scope gates.
- Two-hundred-thirty-sixth verification-chain increment complete: `pnpm run verify:boundaries` now rejects package source relative imports and exports without explicit runtime file extensions, preventing extensionless TypeScript source specifiers from leaking into the ESM package graph before build, pack, or publication.
- Two-hundred-thirty-seventh verification-chain increment complete: `PackageLocalSessionRuntime` now stores package-local kernel port creation and agent kernel creation behind one `PackageLocalRuntimeKernelOperations` bundle instead of splitting the bundle back into private child fields. Topology coverage proves the runtime retains the full `kernelOperations` adapter, routes port and agent-kernel delegation through its `ports` and `agentKernel` facets, and rejects reintroducing `kernelPortOperations` or `agentKernelOperations` fields.
- Two-hundred-thirty-eighth verification-chain increment complete: `pnpm run verify:boundaries` now requires every publishable package export, except the explicit `./package.json` metadata export, to be a condition object with paired `types` and `import` targets. Focused verifier tests prove a package export cannot ship runtime-only or string-only subpaths, keeping public JavaScript and declaration entrypoints aligned before build, pack, or publication.
- Two-hundred-thirty-ninth verification-chain increment complete: `pnpm run verify:boundaries` now requires each publishable package's legacy `main` and `types` fields to match the root export's `import` and `types` targets. Focused verifier tests prove a package cannot publish divergent legacy entry fields and export-map root entrypoints, keeping Node, bundlers, and TypeScript consumers on the same public surface.
- Two-hundred-fortieth verification-chain increment complete: `pnpm run verify:boundaries` now requires every publishable package manifest to declare a root `"."` export condition object. Focused verifier tests prove a package cannot publish only legacy `main` / `types` fields or subpath-only exports, preserving a stable modern entrypoint for Node, bundlers, and TypeScript before pack or publication.
- Two-hundred-forty-first verification-chain increment complete: packed-package and post-publish verifiers now mirror the source root export manifest contract gate. They reject tarball or installed manifests missing a root `"."` condition object, non-`./package.json` exports without paired `types` / `import` conditions, or legacy `main` / `types` fields that drift from the root export targets, keeping package source, packed artifacts, and published npm installs aligned.
- Two-hundred-forty-second verification-chain increment complete: the types-first export condition order gate now covers source, packed-package, and post-publish verifiers for every public export condition object except `./package.json`. This keeps TypeScript declaration resolution ahead of runtime/browser conditions across package source, tarballs, and installed npm artifacts.
- Two-hundred-forty-third verification-chain increment complete: the public export condition allowlist gate now covers source, packed-package, and post-publish verifiers. Public exports may use only `types`, `browser`, and `import` conditions, while the explicit `./package.json` metadata export remains the only `default` condition exception, preserving the ESM-only and browser-stub package contract.
- Two-hundred-forty-fourth verification-chain increment complete: the browser-before-import export condition order gate now covers source, packed-package, and post-publish verifiers. Any public export with a `browser` condition must list it before `import`, preserving browser-condition resolution for SDK stubs when bundlers enable both browser and import conditions.
- Two-hundred-forty-fifth verification-chain increment complete: the manifest target extension gate now covers source, packed-package, and post-publish verifiers. Package `types` and export `types` targets must point at `.d.ts` declaration artifacts, while package `main` plus export `import` / `browser` targets must point at `.js` runtime artifacts, preventing declaration/runtime target swaps from reaching npm.
- Two-hundred-forty-sixth verification-chain increment complete: the public export subpath shape gate now covers source, packed-package, and post-publish verifiers. Public export map keys must be `.` or `./...` and cannot contain parent directory segments, preventing invalid package subpaths or path-like escapes from entering source manifests, tarballs, or installed npm artifacts.
- Two-hundred-forty-seventh verification-chain increment complete: `PackageLocalSessionRuntime` now stores package-local tool filtering, catalog registration, and session custom/builtin registration behind one `PackageLocalRuntimeToolOperations` bundle instead of retaining separate private child fields. Topology coverage proves the runtime delegates through the bundle's `filter`, `registration`, and `sessionRegistration` facets and rejects reintroducing `toolRegistrationOperations`, `sessionToolRegistrationOperations`, or `toolFilterOperations` fields.

Commit:

- `ci: add production verification chain`

### Phase 6: Release Automation

Objective: Publish packages automatically from `main` when all gates pass.

Deliverables:

- semantic-release configured for monorepo packages.
- npm trusted publishing/provenance preserved.
- package changelogs or release notes generated per package.
- release workflow runs `pnpm run verify` before publishing.
- dry-run release workflow or command documented.

Status:

- First release-automation increment complete: semantic-release now uses a fixed-version monorepo release for `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk`. The release workflow still runs `pnpm run verify` before publishing, uses GitHub OIDC trusted publishing without `NPM_TOKEN`, and publishes all three package roots through `@semantic-release/npm`. A local prepare plugin synchronizes each package manifest to `nextRelease.version` and rewrites internal `workspace:*` dependencies to the same concrete version before npm publish. `pnpm run release:dry` remains the tokened release rehearsal path without publishing.
- Second release-automation increment complete: release notes now include a generated per-package section for the fixed-version monorepo release, listing the published `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` versions plus the session-first install command after the conventional commit notes.
- Third release-automation increment complete: the retired manual `scripts/release.js` path and its helper were removed, `release:legacy` is no longer exposed from the root package scripts, and `pnpm run verify:release` now rejects any reintroduction of manual release aliases or legacy release helper files so publishing stays on the semantic-release workflow from `main`.
- Fourth release-automation increment complete: the repository now pins the pnpm toolchain to `pnpm@11.7.0` in root package metadata and every GitHub workflow that installs dependencies, while release verification rejects unpinned release workflow pnpm setup so CI, docs, and main-branch publishing use the same package manager version.
- Fifth release-automation increment complete: `pnpm run verify:release` now copies the real publishable package manifests into a temporary release-prep workspace, runs the semantic-release workspace-version prepare plugin with a synthetic release version, and rejects prepared manifests that still contain `workspace:` or `0.0.0` placeholders so fixed-version monorepo publishing is verified against the actual package metadata.
- Sixth release-automation increment complete: all three publishable packages now declare `publishConfig.provenance: true`, and `pnpm run verify:release` rejects package metadata that would publish without explicit npm provenance enabled, complementing the GitHub OIDC trusted publishing workflow.
- Seventh release-automation increment complete: the repository now exposes `pnpm run verify:published -- --version <version>` as a post-publish visibility check that polls the GitHub Release and all three npm packages, so maintainers can verify public artifacts after the main-branch semantic-release workflow finishes.
- Eighth release-automation increment complete: `pnpm run verify:published -- --version <version>` now installs the released `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` versions into a temporary consumer and runs runtime import smoke checks against the public package exports, proving the external npm install path works after publication.
- Ninth release-automation increment complete: the post-publish verifier now also writes a temporary TypeScript consumer and runs `tsc --noEmit` against public `ModelPort`, `AgentKernelOptions`, `SessionOptions`, `StreamMessage`, and tool declaration imports from the released npm packages, proving the external type surface works after publication.
- Tenth release-automation increment complete: the post-publish verifier now type-checks public subpath declarations from the released npm packages, including `@blade-ai/ai/model`, `@blade-ai/ai/providers/openai-compatible`, `@blade-ai/agent/kernel`, `@blade-ai/agent/ports`, `@blade-ai/agent/protocol`, and the session/tools/core `@blade-ai/agent-sdk` subpaths, proving direct Pi-style package-boundary imports work after publication.
- Eleventh release-automation increment complete: the main-branch release workflow now runs post-publish verification after semantic-release. It refreshes release tags, discovers the latest `v*` tag, and runs `pnpm run verify:published -- --version <tag>` with `GH_TOKEN`, so public GitHub Release, npm package visibility, runtime import smoke, and root/subpath TypeScript declaration checks are part of the automated publishing job instead of only a manual maintainer step.
- Twelfth release-automation increment complete: the post-publish verifier now inspects each released npm package's registry `dist.attestations.provenance.predicateType` and requires the SLSA provenance predicate (`https://slsa.dev/provenance/v1`), so npm provenance attestations are verified after publication rather than only configured in package metadata.
- Thirteenth release-automation increment complete: the release workflow now captures the latest `v*` tag before semantic-release and compares it with the latest tag after semantic-release. Post-publish verification runs only when a new release tag appears, preventing no-release main-branch pushes from re-verifying an older tag that may not match the current three-package publish contract.
- Fourteenth release-automation increment complete: the post-publish temporary consumer now runtime-loads and type-checks the published `@blade-ai/agent-sdk/server`, `@blade-ai/agent-sdk/local`, and `@blade-ai/agent-sdk/browser` subpaths in addition to root, `session`, `tools`, and `core`, keeping the released npm verification aligned with the packed package boundary smoke for server-only, Node-local, and browser-safe entrypoints.
- Fifteenth release-automation increment complete: the post-publish temporary consumer now installs `esbuild`, browser-bundles the released SDK root plus `session`, `server`, `local`, `core`, and `tools` subpaths with the browser condition, executes the resulting bundle, and scans for Node-only markers, proving the published npm artifacts preserve the same browser-safe/server-only boundary verified by packed tarball checks.
- Sixteenth release-automation increment complete: `pnpm run verify:release` now rejects publishable package manifests that are not ESM-only or side-effect-free. Focused release verifier tests prove all three public packages must keep `type: "module"` and `sideEffects: false`, preserving the package shape expected by modern Node, bundlers, and temporary-consumer package checks.
- Seventeenth release-automation increment complete: `pnpm run verify:release` now rejects widened publishable package `files` metadata. Focused release verifier tests prove `@blade-ai/ai` and `@blade-ai/agent` publish only `dist` plus `README.md`, while `@blade-ai/agent-sdk` may additionally publish `vendor/ripgrep/**`, keeping npm tarball scope narrow before pack/install verification runs.
- Supply-chain release-automation increment complete: CI, docs, and release workflows now install dependencies with `pnpm install --frozen-lockfile --ignore-scripts`, and `pnpm run verify:release` rejects workflow dependency installs that omit the frozen lockfile or lifecycle-script suppression. This matches the Pi-style supply-chain hardening posture while keeping release publishing on trusted npm provenance.
- Supply-chain exact-dependency increment complete: root and publishable package manifests now use exact versions for direct external dependencies, with internal `@blade-ai/*` workspace links remaining as `workspace:*` until release prepare rewrites them to the concrete fixed-version release. `pnpm run verify:release` rejects future direct dependency ranges such as `^` or `~`, reducing supply-chain drift before CI, pack, and post-publish consumer checks run.
- Supply-chain build-script allowlist increment complete: `pnpm-workspace.yaml` keeps dependency build scripts behind a three-package allowlist (`@vscode/ripgrep`, `esbuild`, and `node-pty`), and `pnpm run verify:release` rejects any wider allowlist before CI or main-branch release can run. This complements `--ignore-scripts` installs by making any approved dependency build scripts explicit and reviewable.
- Eighteenth release-automation increment complete: `pnpm run verify:release` now rejects missing npm discoverability metadata. Focused release verifier tests prove publishable packages must keep the project homepage, GitHub issues URL, and baseline `agent` / `sdk` / `llm` keywords so all three public packages remain searchable and supportable after automated main-branch publishing.
- Nineteenth release-automation increment complete: `pnpm run verify:release` now rejects release workflows whose `actions/setup-node` version drifts below the package engine floor. Focused workflow tests prove the main-branch publishing job stays pinned to Node `22.14`, matching the `>=22.14.0` engine contract used by all three publishable packages.
- Twentieth release-automation increment complete: `pnpm run verify:release` now validates the CI workflow toolchain and command chain as part of release readiness. Focused workflow tests prove CI remains on Node `22`, pins `pnpm/action-setup` to `11.7.0`, and runs only the frozen install plus full production `pnpm run verify` gate before main-branch publishing can rely on it.
- Twenty-first release-automation increment complete: the post-publish temporary consumer now reads the installed `@blade-ai/agent-sdk/dist/core/index.d.ts` declaration file and rejects server-only session APIs or Node-local adapter APIs inside the browser-safe `core` type surface, matching the packed package declaration boundary check against the actually published npm artifact.
- Twenty-second release-automation increment complete: packed-package and post-publish temporary consumers now type-check the full public `SessionOptions` model sampling and budget surface (`temperature`, `maxOutputTokens`, `maxContextTokens`, `providerOptions`, `thinkingEnabled`, `thinkingBudget`, and `tokenBudget`). A release verifier contract test also requires those fields to stay present in the source type, README, session reference, packed consumer, and published consumer.
- Twenty-third release-automation increment complete: the post-publish temporary consumer now also browser-bundles the published runtime-independent agent browser bundle from `@blade-ai/agent` and `@blade-ai/agent/kernel`, executes it, and scans the output for Node-only markers. This keeps the actual npm artifact aligned with the Pi-style agent-core boundary, not just the local packed tarball smoke.
- Twenty-fourth release-automation increment complete: the release verifier now rejects package READMEs without direct install/import snippets for each publishable package. `pnpm run verify:release` therefore checks that `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` remain self-describing as standalone npm packages before semantic-release can publish from `main`.
- Twenty-fifth release-automation increment complete: the post-publish verifier now includes a published package README gate. After installing the released packages into a temporary consumer, `pnpm run verify:published -- --version <version>` reads the installed `node_modules/@blade-ai/ai/README.md`, `node_modules/@blade-ai/agent/README.md`, and `node_modules/@blade-ai/agent-sdk/README.md`, then rejects artifacts missing direct install or import snippets and requires published README 与 package README 完全一致. This proves the npm-visible README content is self-describing and unchanged from the source package docs, not only the source tree metadata.
- Packed package README gate complete: `pnpm run verify:packages` now includes a packed package README gate. After `pnpm pack`, it extracts `package/README.md` from each tarball and rejects packed package READMEs missing the package name, direct install command, minimal import snippet, or an exact source README match. This catches publishable README drift before the artifacts reach semantic-release or npm, and documents that packed README 与 package README 完全一致.
- packed package file-scope gate complete: `pnpm run verify:packages` now rejects packed tarballs that include source files under `package/src` or nested `/src/` paths, matching the post-publish installed package file-scope gate before artifacts reach npm.
- TypeScript artifact-scope gate complete: packed and post-publish package file-scope verification now rejects `.ts`, `.tsx`, `.mts`, `.cts`, `tsconfig*.json`, and `tsup.config.*` artifacts while still allowing built `.d.ts` declarations, keeping npm artifacts focused on compiled runtime and public type output.
- package artifact allowlist gate complete: packed and post-publish package verification now rejects artifacts outside `package.json`, `README.md`, `LICENSE`, `dist/**`, and the explicit `@blade-ai/agent-sdk` `vendor/ripgrep/**` exception, preventing docs, configs, source folders, or accidental assets from entering npm packages.
- packed package manifest hygiene gate complete: publishable package source manifests no longer carry `private` or `devDependencies`, and `pnpm run verify:packages` rejects packed tarball manifests that expose those fields. This makes the local tarball smoke match npm-facing manifest hygiene before release preparation or post-publish verification runs.
- package lifecycle script gate complete: source, packed, and post-publish manifest verification now rejects npm lifecycle scripts such as `preinstall`, `install`, `postinstall`, `prepare`, `prepublish`, and `prepublishOnly`. This keeps publishable library packages free of automatic install/publish hooks while still allowing ordinary development scripts in source manifests.
- manifest target existence gate complete: packed and post-publish manifest verification now confirms every `main`, `types`, and recursive `exports` target exists in the tarball or installed package. This catches stale manifest pointers that satisfy shape and extension policy but would fail in real consumers.
- runtime external dependency declaration gate complete: packed and post-publish manifest verification now scans `dist/**/*.js` runtime imports and rejects bare external package imports that are not declared in the artifact's runtime dependency sections. This catches undeclared production dependencies after bundling, packing, and installation rather than only in source manifests.
- runtime relative import resolution gate complete: packed and post-publish manifest verification now scans `dist/**/*.js` relative import/export specifiers and rejects references that cannot resolve to package-local JavaScript files or that escape the package directory. This catches broken emitted chunk graphs before or after npm publication.
- declaration relative reference resolution gate complete: packed and post-publish manifest verification now scans `dist/**/*.d.ts` relative import/export/import-type references and triple-slash reference paths, then rejects references that cannot resolve to package-local declaration files or that escape the package directory. This catches broken public type graphs before or after npm publication.
- declaration external dependency declaration gate complete: packed and post-publish manifest verification now scans `dist/**/*.d.ts` bare external import/export/import-type references and triple-slash type references, then rejects external type dependencies that are not declared in the artifact's runtime dependency sections. This catches missing npm-visible type dependencies before or after publication, even when public smoke tests do not traverse every declaration file.
- packed package dependency-version gate complete: `pnpm run verify:packages` now rejects external dependency ranges and `0.0.0` placeholders in packed tarball manifests. Internal `@blade-ai/*` local pack placeholders remain covered by release prepare and post-publish manifest checks, while npm-facing external dependencies are pinned before artifacts reach semantic-release.
- packed internal dependency-version gate complete: `pnpm run verify:packages` now rejects internal `@blade-ai/*` dependency ranges in packed tarball manifests, allowing only the local-pack `0.0.0` placeholder or a concrete exact version. This keeps internal monorepo dependencies from drifting in tarballs while still respecting the fixed-version release prepare step.
- published package dependency-version gate complete: `pnpm run verify:published` now rejects external dependency ranges from the installed npm package manifests, while still requiring internal `@blade-ai/*` dependencies to match the released fixed version. This closes the dependency-version loop from source manifests to packed tarballs to actual user-installed artifacts.
- post-publish temporary consumer toolchain pin gate complete: the post-publish verifier now installs its temporary consumer TypeScript and esbuild tooling with exact versions instead of ranges, so published package type and browser-bundle smoke checks do not drift with npm resolution while validating a release.
- Twenty-sixth release-automation increment complete: the post-publish verifier now includes a published package manifest gate. After installing the released packages into a temporary consumer, `pnpm run verify:published -- --version <version>` reads each installed package manifest, rejects version mismatches, `workspace:` protocols, `0.0.0` placeholders, and internal `@blade-ai/*` dependency versions that do not match the released fixed version. This proves semantic-release manifest preparation is reflected in the npm-visible artifacts, not only in the source-tree dry run.
- Twenty-seventh release-automation increment complete: the post-publish verifier now includes a published package manifest entry target gate. After installing the released packages into a temporary consumer, it recursively checks installed `main`, `types`, and `exports` targets, allowing only `./dist/*` artifacts and the explicit `./package.json` metadata export while rejecting source-file targets, absolute/non-package-relative targets, and package escapes. This proves the npm-visible entrypoint map stays aligned with the built artifacts after publication.
- Published SDK browser export condition gate complete: the post-publish verifier now rejects published `@blade-ai/agent-sdk` manifests whose root browser condition no longer resolves to `./dist/browser/index.js`, or whose `server`, `session`, and `local` browser conditions no longer resolve to the explicit server-only stub. The published SDK browser export condition gate catches npm artifact regressions that would otherwise drag server/local dependencies into browser consumers after publication.
- Twenty-eighth release-automation increment complete: the post-publish verifier now includes a published package file-scope gate. After installing the released packages into a temporary consumer, it scans each installed package directory and rejects declaration maps, JavaScript source maps, test files, and source files. This extends the packed-tarball file-scope rules to the actual npm-installed artifacts users consume.
- Twenty-ninth release-automation increment complete: the post-publish verifier now includes a published package npm metadata gate. After installing the released packages into a temporary consumer, it rejects npm-visible manifests whose `license`, `homepage`, `bugs`, or `repository` metadata drift from the source release contract, keeping published artifacts traceable and supportable after semantic-release preparation.
- published package repository directory metadata gate complete: source, packed-package, and post-publish manifest checks now require each publishable package `repository.directory` to match its `packages/*` workspace directory, so npm-visible package metadata points back to the correct monorepo source path for `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk`.
- Thirtieth release-automation increment complete: `pnpm run verify:release` now keeps the workspace root as a private non-publishable orchestrator. The release gate rejects root `publishConfig` / `files` metadata and any semantic-release npm plugin configuration that would publish the workspace root, ensuring main-branch publishing can only target `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk`.
- Thirty-first release-automation increment complete: the post-publish verifier now checks npm registry distribution metadata before trusting a release. `pnpm run verify:published -- --version <version>` requires each package's `latest` dist-tag to point at the released version, validates registry tarball `integrity` and `shasum` metadata, and cross-checks the temporary consumer `package-lock.json` resolved tarball URL and SRI integrity against the registry metadata.
- Thirty-second release-automation increment complete: `pnpm run verify:release` now requires the release workflow to serialize main-branch publishing through `concurrency.group: release-main` while keeping `cancel-in-progress: false`. This prevents overlapping semantic-release publish jobs without cancelling an in-flight npm publish or post-publish verifier.
- Thirty-third release-automation increment complete: the release workflow now upgrades trusted publishing with exact `npm@11.5.1`, and `pnpm run verify:release` rejects range-based npm CLI upgrades. This keeps the most sensitive publish-time tool pinned just like direct dependencies, pnpm, and the Node engine floor.
- Thirty-fourth release-automation increment complete: the trusted-publishing npm CLI upgrade now runs with `--ignore-scripts`, and `pnpm run verify:release` rejects npm CLI upgrade steps that would allow lifecycle scripts before package publication. This extends the workflow supply-chain policy beyond workspace installs to the publish-time tool bootstrap itself.
- Thirty-fifth release-automation increment complete: `pnpm run verify:release` now requires the release workflow checkout step to use `fetch-depth: 0`, preserving full git history and tags for semantic-release release notes, tag discovery, and post-publish version comparison.
- Release workflow shape gate complete: `pnpm run verify:release` now requires the release workflow to support manual `workflow_dispatch`, run on `ubuntu-latest`, keep a 20 minute timeout, use `actions/checkout@v5`, and keep `actions/setup-node` caching pnpm. This makes the main-branch publishing job shape explicit alongside permissions, concurrency, and command-order gates.
- source package version placeholder gate complete: all three publishable source package manifests now keep `version: "0.0.0"` until semantic-release prepares a concrete fixed-version release, and `pnpm run verify:release` rejects source version drift before main-branch publishing can run.
- CI least-privilege workflow permission gate complete: the CI workflow now grants only `contents: read`, and `pnpm run verify:release` rejects workflow drift that would let ordinary verification jobs inherit broader repository token permissions.
- CI workflow shape gate complete: `pnpm run verify:release` now requires CI to run on the expected main/master/refactor/codex branch pushes and pull requests, use `ubuntu-latest`, keep a 20 minute timeout, use `actions/checkout@v5`, and keep `actions/setup-node` caching pnpm. This makes the ordinary quality gate entrypoint as explicit as the release and docs workflows.
- Release workflow permission gate complete: `pnpm run verify:release` now requires the release workflow to grant exactly `contents: write`, `issues: write`, `pull-requests: write`, and `id-token: write`, preserving semantic-release/GitHub Release/trusted-publishing capability without allowing unrelated repository token scopes.
- Docs workflow permission gate complete: `pnpm run verify:release` now requires the docs deployment workflow to grant exactly `contents: read`, `pages: write`, and `id-token: write`, preserving GitHub Pages deployment while preventing broader repository token permissions.
- Docs workflow toolchain gate complete: `pnpm run verify:release` now also requires the docs deployment build job to pin `pnpm/action-setup` to `11.7.0`, run on Node `22`, execute only the frozen install plus `pnpm run docs:build`, and upload `docs/.vitepress/dist` as the Pages artifact. This keeps the production docs path covered by the same Pi-style workflow drift checks as CI and release.
- Docs workflow deployment gate complete: `pnpm run verify:release` now requires docs deployment to trigger only from `main` docs/workflow changes or manual dispatch, serialize through `concurrency.group: pages` without cancelling in-flight deploys, and run an `actions/deploy-pages@v4` deployment job against the `github-pages` environment only after the build job. This closes the docs publishing path beyond permissions, build tooling, and artifact location.
- npm-facing manifest hygiene gate complete: release preparation now removes `private` and `devDependencies` metadata from publishable package manifests, while `verify:release` and `verify:published` reject prepared or installed manifests that expose those fields. This keeps source workspace metadata separate from npm-facing package metadata.
- package license artifact gate complete: source, packed-package, and post-publish license verification now compares each package `LICENSE` artifact with the repository root `LICENSE` byte-for-byte after confirming the MIT permission grant, so npm-visible package metadata cannot drift from the source license. The README and production checklist document that source package LICENSE 与根 LICENSE 完全一致 and LICENSE 与根 LICENSE 完全一致.

Commit:

- `ci: publish monorepo packages from main`

### Phase 7: Documentation and Examples

Objective: Make the new architecture clear for users and agents.

Deliverables:

- Architecture guide.
- Package guide.
- Session-first quickstart.
- Provider guide.
- Tool authoring guide.
- Server/Next.js guide.
- Browser-safe/remote-client guide.
- Production checklist.

Status:

- First documentation increment complete: VitePress now includes an architecture guide and package/entrypoint guide. The architecture guide documents the `@blade-ai/ai` / `@blade-ai/agent` / `@blade-ai/agent-sdk` responsibilities, dependency direction, runtime boundaries, observability boundaries, and production verification gates. The package guide documents recommended imports, Browser-safe vs Server / CLI boundaries, direct `@blade-ai/ai` provider usage, runtime-independent `@blade-ai/agent` usage, and session-first `@blade-ai/agent-sdk` usage.
- Second documentation increment complete: a session-first quickstart now documents the server/CLI `createSession()` flow, explicitly calls out `allowedTools: []` as disabling all tools, links to `examples/session-first-server.ts`, and adds `pnpm run verify:examples` to the root verification chain so the public quickstart import remains type-checked.
- Third documentation increment complete: a Server / Next.js guide now documents Route Handler and Server Action usage, keeps root `createSession()` server-only, requires `runtime = 'nodejs'` for Route Handlers, and tells browser clients to communicate over HTTP while importing only browser-safe `core` / `tools` contracts.
- Fourth documentation increment complete: a Browser Remote Client guide now documents browser-safe `core` / `browser` imports, remote HTTP NDJSON streaming with `StreamMessage`, server-only stub behavior, and the production boundary that keeps provider keys, tools, MCP, filesystem, shell, and sandbox execution on the server.
- Fifth documentation increment complete: a Production Checklist now documents the required `pnpm run verify` release gate, package boundary checks, browser-safe entrypoint checks, optional GLM live smoke, release dry-run, and trusted publishing flow from `main`.
- Sixth documentation increment complete: a Provider and model guide now documents session-first provider configuration, direct `@blade-ai/ai` `ModelPort` usage, OpenAI-compatible/GLM smoke testing, Vercel provider adapters, stream events, usage normalization, and the boundary that keeps `@blade-ai/agent` independent of provider SDKs.
- Seventh documentation increment complete: a Tool Authoring guide now documents production custom tool design for session-first apps, browser-safe tool imports, `defineTool` / `createTool`, `ExecutionContext`, allowed-tool policy, permission update effects, stream event mapping, and verification expectations.
- Eighth documentation increment complete: the root README now presents the Pi-style three-package architecture as installable public packages, documents the recommended `@blade-ai/ai` / `@blade-ai/agent` / `@blade-ai/agent-sdk` import boundaries, and surfaces the production verification chain including packed tarball temporary-consumer smoke, browser/entrypoint checks, default integration skip behavior, and explicit GLM live commands.
- Ninth documentation increment complete: examples now cover all three public package layers. `examples/ai-model-port.ts` demonstrates direct `@blade-ai/ai` `ModelPort.generate()` usage against OpenAI-compatible / GLM configuration, `examples/agent-kernel.ts` demonstrates runtime-independent `AgentKernel.runTurn()` with an injected model port, and `pnpm run verify:examples` type-checks those examples alongside the session-first server example.
- Tenth documentation increment complete: each publishable package README now includes a direct `pnpm add` command and minimal import usage for its public package. The package metadata contract test requires those install/import snippets to stay present before npm publication, keeping `@blade-ai/ai`, `@blade-ai/agent`, and `@blade-ai/agent-sdk` self-describing when viewed outside the monorepo.
- Eleventh documentation increment complete: the README, package guide, API reference, tools guide, recipes, MCP guide, and package README now document the Pi-style root/local split: root remains session-first for server and CLI code, while builtin tools, in-process MCP helpers, filesystem memory, and sandbox adapters must be imported from `@blade-ai/agent-sdk/local`.
- Twelfth documentation increment complete: the README, package guide, architecture guide, production checklist, provider guide, and package README now document the provider-helper split: `@blade-ai/agent-sdk` root remains session-first, while DeepSeek cost/cache/long-context/runtime helpers are imported from `@blade-ai/ai/deepseek`.
- Thirteenth documentation increment complete: the README and package guide now document `@blade-ai/agent-sdk/server` as an explicit server-only facade that composes session/core/tools/subagent exports without wildcard-forwarding through root, matching the packed and post-publish verifier guardrails.
- Fourteenth documentation increment complete: the README and package guide now clarify that `@blade-ai/agent-sdk/server` stays aligned with the root entry's server-safe runtime/type surface while remaining independently auditable as an explicit facade.

Commit:

- `docs: document production agent sdk architecture`

## Completion Criteria

The migration is complete only when all of the following are true:

- The repo has `packages/ai`, `packages/agent`, and `packages/agent-sdk`.
- `@blade-ai/agent-sdk` still supports session-first `createSession()`.
- `@blade-ai/agent` is proven free of Node-local runtime dependencies.
- `@blade-ai/ai` owns provider execution and usage normalization.
- Existing session, tools, hooks, observability, permission, and token budget behavior is covered by tests.
- CI verifies lint, type-check, build, unit, integration, package exports, browser safety, docs, and pack/install.
- Pushes to `main` can publish automatically after gates pass.
- Documentation describes the package boundaries and recommended usage.
