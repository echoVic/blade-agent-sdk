# 更新日志

`@blade-ai/agent-sdk` 的所有重要变更都记录在此。

## [7.4.3] - 2026-09-13

### 修复

- 不带 toolPolicy 的 turn 级 Skill 补丁保留 session 策略基线，临时 Skill 清理后会话限制不再丢失。Skill 注册表按包含 trustLevel、shellPolicy、hookPolicy 在内的完整配置缓存；共享存储上的子任务孤儿回收只处理所属父会话的后代。
- session.read 的恢复快照现在返回事件日志头部序号（不会早于同一响应中的消息快照），并在 Session 未加载时省略待处理输入计数，不再把未知表现为 0。浏览器 SSE 解析器按 UTF-8 字节对每个完整帧和未完成的剩余帧分别执行 4 MiB 上限。
- 流式模型请求现在会重试首个输出之前的失败，包括 AI SDK 在流内上报的提供方错误。输出已交付后流终止，并保留提供方原始错误（含状态码）作为 cause，不再退化为通用提示。
- 队列提交恢复现在绑定原始命令身份：输入、命令或执行选项不同的重试会被明确拒绝，而不是静默替换待恢复请求；接受记录在任何后续失败之前先行写入。启动对账还会在入队前的接受记录丢失时，直接从 Session 日志重建该记录。
- 仅在路由以同一请求和 attempt 结算并释放租约后才补发已记录的终态结果，客户端不会再看到「请求已完成但下一轮输入仍被拒绝」。路由已切换到新请求的旧结果会被标记取代而不发布；Worker 与启动对账器在追加事件前都会先检查事件日志去重。

### 文档

- 补充文档：恢复快照的读取边界、流式重试范围、Skill 注册表缓存身份、共享存储上的后台子任务所有权，以及生产示例的提交与终态结果对账契约。

## [7.4.2] - 2026-09-13

### 重构

- 发布版本号改为由推送的 tag 决定：发布工作流原样发布该 tag 的版本号（双语变更日志不变），不再由 commit 类型推导 bump；只推 main 不再触发发布。

## [7.4.1] - 2026-09-12

### 重构

- 生产示例改为从 PostgreSQL 路由状态读取崩溃恢复边界，不再在启动器中缓存 Worker 通知：后继进程看到同一个已提交检查点，恢复状态不再存放在存储之外。
- 把工具执行拆成显式阶段（中间件、Hook、授权、确认、文件锁、调用、结果归一化），并由统一的清理守卫负责隔离与取消记账。工具行为、权限决策与超时语义保持不变。

### 文档

- 补充文档说明 production starter 中哪些部分由 SDK 负责（路由队列、租约、恢复计划、检查点），哪些属于你自己的仓库策略，避免把复制进来的模块重新实现一遍。

## [7.4.0] - 2026-09-12

### 新功能

- session.read 返回恢复快照，包含路由状态、待处理输入与最后事件序号，重连的客户端不再依赖浏览器本地存储来恢复。

## [7.3.0] - 2026-09-12

### 新功能

- 子 Agent 会话存储改为可注入的能力：父会话使用共享仓库时，子 Agent 也能获得同等的跨机恢复能力；并说明默认文件存储仅限本机。

## [7.2.1] - 2026-09-12

### 重构

- 两种工具入口改用同一个内部装配器构建，createTool 与 toolFromDefinition 不再各自维护一份装配逻辑，差异仅保留在参数声明与校验上。

## [7.2.0] - 2026-09-12

### 新功能

- 新增 ExecutionHost.reclaim()：后继进程无需了解后端资源命名即可清理自己从未创建的执行；production 示例改用它，不再直接删除容器。

## [7.1.10] - 2026-09-12

### 重构

- 退役 ContextManager 中无生产调用的路径：第二套格式化与压缩缓存流程、会话搜索，以及每次工具调用都写入却从不读取的工具结果缓存。

## [7.1.9] - 2026-09-12

### 修复

- Session 取消时终止 Skill 内联命令；SSE 帧缓冲加上限，不再因对端始终不发分隔符而无限增长。

## [7.1.8] - 2026-09-12

### 修复

- MCP 健康检查改为真实探测服务器而非读取缓存；在检查进行中调用 stop() 后监控真正停止；流式事件缓冲加上限并做无损文本合并。

## [7.1.7] - 2026-09-12

### 修复

- 以记忆文件为权威来源并由文件重建索引，使并发保存不再丢失条目或让已存记忆不可见；存储失败不再被当作「记忆不存在」。

## [7.1.6] - 2026-09-12

### 修复

- 模型流中上报错误时抛出失败，不再把部分文本当作完整响应返回；关闭 AI SDK 自带重试，由 SDK 独占重试职责。

## [7.1.5] - 2026-09-12

### 修复

- Skill 发现按项目隔离，不再固化首个调用方的配置；Skill 列表按执行所在的工作目录解析。

## [7.1.4] - 2026-09-12

### 修复

- 沙箱策略改为按 Session 作用域，不再使用进程级单例；turn 结束时恢复 session 的工具策略；复制可变运行时数据，避免后续配置修改影响已创建的快照。

## [7.1.3] - 2026-09-12

### 修复

- 对始终未上报结果的 sealed 命令给出可审计的终止状态而非永久 in_progress；仅在执行环境确认停止后才确认取消；SessionRunner 的终态结果改为路由结算后发布。

## [7.1.2] - 2026-09-12

### 修复

- Session 读取不再把「本进程未加载」当作空会话；production 示例在启动时对账「已接受但未入队」的提交与「已记录但未发布」的终态结果。

## [7.1.1] - 2026-09-12

### 重构

- 发布版本改由最新的 v* tag 及其后的 conventional commits 决定；变更片段只决定变更日志分节，不再参与版本判定。

## [7.1.0] - 2026-09-12

### 新功能

- 降低首次任务门槛：create-blade-agent 默认生成 local starter，defineTool 支持 Zod 参数并将省略的 sideEffect 保守默认为 non_idempotent，并新增从 production 示例迁移到自有仓库的教程。

## [7.0.6] - 2026-09-12

### 文档

- 按代码修正运行时文档：effect 错误语义、session fork 所需 scope、四元组审批隔离、必填的 network 策略、cursor 与终结细节、Worker 默认值，以及中英文之间的差异。

## [7.0.5] - 2026-09-12

### 文档

- 更新用户文档：production 预设按当前真实的 SDK Session 与 Docker 仓库工作流描述，补充 SessionRunResult 的终结契约，并说明变更片段类型如何决定发布版本。

## [7.0.4] - 2026-09-12

### 修复

- 升级开发工具链：Vitest 升至 5、VitePress 升至基于 Vite 8 的 2.0 预览版，并锁定已修复的传递依赖版本，使完整依赖审计不再报告任何已知漏洞。

## [7.0.3] - 2026-09-12

### 修复

- 修复生产依赖安全审计：升级 AI SDK 依赖并锁定已修复的 hono、js-yaml、qs 版本，同时修正包清单让 CLI bin 在安装后正确注册。
- 修复 Docker 命令未读取 stdin 即退出时抛出的未捕获 broken pipe 错误，并让仓库 fixture 测试写入 stdin 时不再与命令退出竞争。

## [7.0.2] - 2026-09-12

### 修复

- 修复 Web 示例多轮回答重复，支持刷新和重连后恢复当前对话，并提供明确的取消反馈。
- 修复 SDK Worker 输出的请求关联和优雅移交，仅在带执行权校验的路由结算成功后触发结果终结回调。

### 重构

- production 预设接入真实仓库编辑、审批和测试，支持持久化 SDK Session、Docker Worker 及浏览器恢复。

## [7.0.1] - 2026-08-28

### 修复

- 为不可裁剪事件流增加配额，并串行化 lease、模型 handoff、Worker 转移与审批状态变更。
- 强化文件版本跟踪、快照、原子本地持久化、CRLF 偏移、Unicode 截断与编辑反转义。
- 强化 WebFetch、Hook 输出、OAuth、远程审批、租户身份、Shell 分类与 MCP 工具命名空间的不可信输入边界。

### 性能优化

- 限制后台输出、文件跟踪、锁等待、调度队列与任务持久化开销。

## [7.0.0] - 2026-08-28

### 修复

- 为 Agent 生命周期误用提供明确错误，在销毁时释放自有资源，并正确保留 MCP 服务来源标识。
- 在轮次上限压缩后保留 Agent 累计轮次，并持久化 Provider 特有的非文本用户内容。
- 移除 ModelMessage.metadata，引入类型明确的 ConversationMessage provenance、correlation、telemetry、providerOptions 与 extensions 字段。
- 强制执行规范化文件系统根目录边界，限制压缩输入，隔离 Shell 环境，保守分类 Bash 命令，并串行化 Worker 租约续期与路由状态转换。

## [6.0.5] - 2026-08-26

### 修复

- 为 create-blade-agent 增加依赖极简的 local 与 Web preset，同时保留 production 默认行为。

## [6.0.4] - 2026-08-26

### 修复

- 将完整故障恢复 RTO、吞吐、事件丢失与四点非幂等故障指标纳入发布门禁，并保留 CI 报告。

## [6.0.3] - 2026-08-26

### 修复

- 提供 create-blade-agent、五分钟完整链路验收，以及生成项目的依赖安全审计。

## [6.0.2] - 2026-08-26

### 修复

- 新增带鉴权的 runtime 健康检查、队列指标、uncertain effect 对账与不采集 payload 的 Worker OpenTelemetry。

## [6.0.1] - 2026-08-26

### 修复

- 新增单命令 Web 到 Docker 生产闭环示例及自动化端到端 smoke 验证。

## [6.0.0] - 2026-08-26

### 破坏性变更

- 新增生产级 AgentWorker、SessionRunner、EffectDispatcher 与 ExecutionHost 恢复闭环；将 PostgreSQL 和 OpenTelemetry adapter 移至显式可选入口。

## [5.4.2] - 2026-08-25

### 重构

- 以领域归属明确的模型类型、branded identifiers、显式持久化端口和新的 browser-safe /model 入口，替换重叠的 chat、stream、transcript 与 tool 契约。

## [5.4.1] - 2026-08-25

### 修复

- 即时可用的 Runtime effect 改用 PostgreSQL 事务时钟，避免宿主与数据库时钟偏差导致 worker 延迟领取。

### 重构

- 新增 ExecutionHost 边界、Docker 隔离参考实现、有界 workspace 与网络出口、checkpoint，以及仅作用于单次 command 的短期凭据注入。

## [5.4.0] - 2026-08-25

### 重构

- 新增 PostgreSQL worker heartbeat、Session 路由与 fencing、drain/handoff/preemption，以及崩溃安全的 effect 恢复。

## [5.3.13] - 2026-08-25

### 重构

- 新增 PostgreSQL Runtime Store，原子提交 command、event、effect 与 projection，并提供公开 conformance suite。

## [5.3.12] - 2026-08-25

### 重构

- 将 Session 执行提取为可注入的 SessionExecutor 与 InProcessSessionExecutor 契约。

## [5.3.11] - 2026-08-25

### 重构

- 新增可注入 Session repository、protocol v1，以及支持租户隔离的 AgentServer 与浏览器 AgentClient HTTP/SSE 运行时。

## [5.3.10] - 2026-08-25

### 重构

- 将运行时默认能力拆分为无隐式宿主访问的服务端入口和本地 Node.js 入口，并以 node 入口替代 local。

## [5.3.9] - 2026-08-25

### 修复

- 新增实例级 Provider Registry 以接入自定义模型 adapter，并在 Session、subagent 与 compaction 链路中提供 fail-closed 路由。

## [5.3.8] - 2026-08-25

### 修复

- 持久化 assistant 历史的 Provider、API adapter 与模型来源，并在跨 Provider 或模型回放时安全降级 reasoning。

## [5.3.7] - 2026-08-25

### 修复

- 为 durable Journal、subscription、JSONL 与 execution lease Store 调用增加协作取消和类型化 fail-closed 超时。

## [5.3.6] - 2026-08-23

### 修复

- 阻止已取消的文件 Hook 启动进程，将后代进程纳入 SDK 持有的 POSIX 进程组或 Windows Job，并在无法确认清理完成时保持 fail-closed。

## [5.3.5] - 2026-08-23

### 修复

- 工具等待并发槽位或同文件锁时现在响应当前 Request 的取消信号，且不会泄漏 lease 或破坏 FIFO 顺序。

## [5.3.4] - 2026-08-23

### 修复

- 权限与确认等待现在响应当前 Request 的取消信号；若回调忽略取消，Session 会保留执行所有权直至清理完成。

## [5.3.3] - 2026-08-23

### 修复

- 为 Session inline hook 事件增加可配置时限，将取消信号传入 callback，并在超时 callback 尚未结束时保持 fail-closed。

## [5.3.2] - 2026-08-23

### 修复

- 工具调用默认限制为 10 分钟，新增 SessionOptions.toolTimeoutMs，确保 progress yield 期间持续计时，并在超时清理未完成时保持 fail-closed。

## [5.3.1] - 2026-08-23

### 修复

- 为停滞的模型调用增加可配置的请求与流空闲超时，主动中止 provider 并保留类型化失败语义。

## [5.3.0] - 2026-08-23

### 新功能

- 新增 lease-aware 洋葱式模型与工具 middleware、声明式插件、durable 短路结算及子 Agent 传播。

## [5.2.0] - 2026-08-23

### 新功能

- 新增 Store-backed Session 执行租约：支持自动 heartbeat、粘性单调 fencing、Journal、transcript 与 subagent 状态原子保护、进程树取消及 close/handoff 受控释放。

## [5.1.1] - 2026-08-23

### 修复

- 升级 CI、发布与文档工作流使用的 GitHub Actions，统一迁移到 Node.js 24 runtime。

## [5.1.0] - 2026-08-23

### 新功能

- 新增受控 Session worker handoff 屏障：封闭后台工作准入、等待本地执行收敛、保留未完成的 durable frontier，并在不关闭 durable Session 的情况下返回恢复计划。

## [5.0.4] - 2026-08-23

### 修复

- 使运行中 Session 的 abort 与 close 操作在无消费者死锁的情况下，等待 Agent stream 清理、模型与工具收敛及请求所有权释放，并在配置 durableEventStore 时等待 Request 终态持久化。

## [5.0.3] - 2026-08-23

### 修复

- 跨进程串行化本地 Session transcript 访问，持久同步追加写入，恢复崩溃造成的残缺尾部，拒绝路径不安全的 Session ID，并对已提交记录损坏执行 fail-closed。

## [5.0.2] - 2026-08-23

### 修复

- 移除未使用的生产依赖、升级存在漏洞的依赖，并在 CI 中阻止已知生产依赖漏洞。

## [5.0.1] - 2026-08-23

### 修复

- 通过有界等待和崩溃时由操作系统释放的文件锁，在多个 Node.js 进程之间串行化 JSONL durable event 读取与 compare-and-append 写入。

## [5.0.0] - 2026-08-23

### 破坏性变更

- 在 provider 调用前后持久化并确定性闭合模型请求尝试，将 durable 工具调度绑定到权威模型响应，对未知结果强制显式对账，并将 durable event wire format 升级至 schema v3，同时保持 schema v2 读取兼容。

## [4.4.0] - 2026-08-22

### 新功能

- 新增首个 Turn 前崩溃 Request 的原子恢复，为 steering 输入实施先持久化后准备与过期边界熔断，并支持显式 Request 终态对账。

## [4.3.0] - 2026-08-22

### 新功能

- 新增 active Turn 到 provenance continuation Request 的原子 rollover，并提供 stale-CAS 防护与非幂等边界的 fail-closed 保证。

## [4.2.0] - 2026-08-22

### 新功能

- 新增拉取式 durable 事件订阅，支持可验证的重连 cursor、回放边界、有界缓冲和实时交付。

## [4.1.0] - 2026-08-22

### 新功能

- 新增 Durable Recovery Coordinator，支持幂等的工具结果与权限对账，并自动恢复已接受但尚未开始执行的请求。

## [4.0.0] - 2026-08-22

### 破坏性变更

- 要求每个工具显式声明 pure、idempotent 或 non_idempotent 副作用语义；durable event schema v2 持久化最终执行输入与解析后的契约，据此区分可重放工作与必须对账的未知结果。

### 修复

- 让 changelog fragment 直接驱动 semantic-release 版本选择，并识别 Conventional Commit 的感叹号 header，防止 breaking change 合并后未发布版本。

## [3.3.0] - 2026-08-22

### 破坏性变更

- 将 fail-closed、opt-in durable 事件日志接入 Session 的请求、Turn、工具、权限、中止、关闭与流取消生命周期；Session.abort() 现返回 Promise，以等待 pending request 的 durable 终态。

### 新功能

- 新增面向 command 的 durable Session journal，提供生命周期预演、有界 CAS 重试、幂等 replay 与未知写入结果对账。
- 新增严格的逐事件 durable 生命周期 payload 与确定性 Session 恢复投影，显式区分工具结果对账和待决权限状态。
- 新增可等待的工具生命周期钩子，在调度、权限交互、副作用开始和终态结果发布之间强制 durable 顺序。

## [3.2.0] - 2026-08-22

### 新功能

- 新增 Durable Event Store 第一阶段，提供版本化事件信封、compare-and-append 序列、cursor 读取和可容忍尾部中断写入的 JSONL adapter。

## [3.1.2] - 2026-08-22

### 修复

- 允许 Session 自定义工具直接接收 `createTool()` 和 Memory helper 返回的完整 `Tool` 实例。

## [3.1.1] - 2026-08-22

### 修复

- 启用 Sandbox 但系统缺少受支持的沙箱执行器时拒绝运行，避免静默降级。

## [3.1.0] - 2026-08-22

### 新功能

- 新增持久化的 `now`、`next`、`later` Session 输入，支持安全点转向和可中断工具执行。

### 文档

- 恢复自动生成的更新日志，并补齐完整的英文和简体中文文档。

## [3.0.0] - 2026-08-22

### 破坏性变更

- 要求所有工具的 `execute` 函数返回 `ToolExecution`。
- 使用结构化的 `status`、`model` 和 `display` 契约替代旧的 `success`、`llmContent` 和 `displayContent` 结果字段。

### 重构

- 新增结构化的工具进度、消息和副作用流。
- 统一工具并发限制，并按能力拆分内置工具。
- 惰性初始化文件设施，并使本地 workspace 发现可选。

## [2.0.1] - 2026-08-21

### 修复

- 通过显式请求阶段阻止 `send()` 与 `stream()` 并发调用。

## [2.0.0] - 2026-08-21

### 破坏性变更

- 使用 `outcome` 判别字段替代 `SandboxCheckResult` 的布尔字段，并将 `reason` 改为必填。

### 性能优化

- 缓存已知会话，避免每次持久化写入都扫描全部 JSONL 文件。

## [1.1.3] - 2026-08-21

### 修复

- 恢复持久化会话时保留工具结果。

## [1.1.2] - 2026-08-21

### 修复

- MCP 自动重连成功后清除过期的 `lastError` 状态。

## [1.1.1] - 2026-08-16

### 修复

- 保留网页抓取工具的中止错误分类。

## [1.1.0] - 2026-07-02

### 新功能

- 通过 `tools` 容器和泛型 `defineTool` data 改善自定义工具类型推导。

### 修复

- 重新导出 `JsonObject` 和 `JsonValue`，并修正 ESM 与工具文档。
- 声明 npm provenance 使用的 GitHub 仓库。

## [1.0.12] - 2026-06-01

### 新功能

- 新增浏览器安全和仅服务端的包子路径导出。

## [1.0.11] - 2026-06-01

### 新功能

- 新增 Session token 预算配置。
- 新增模型参数配置并完善工具白名单处理。
- 新增 Session 可观测性追踪。

## [1.0.10] - 2026-05-30

- 无面向用户的变更。

## [1.0.9] - 2026-05-30

### 新功能

- 新增原生 DeepSeek Provider、思维输出处理和工具调用兼容。
- 新增 DeepSeek 缓存统计、成本优化、长上下文规划和批量汇总。

## [1.0.8] - 2026-04-23

### 新功能

- 新增工具惰性加载和类型化 SDK 错误。

### 重构

- 引入 branded identifier 并拆分工具类型定义。
- 改进生命周期清理并移除未使用的内部 API。

## [1.0.7] - 2026-04-18

### 新功能

- 新增工具执行并发调度。

### 重构

- 重构流式响应和事件队列处理。

## [1.0.6] - 2026-04-17

### 重构

- 重构流式响应和事件队列处理。

## [1.0.5] - 2026-04-14

### 重构

- 将消息数组改为 readonly，并统一 JSON value 类型。
- 使用 `metadata.summary` 替代 `displayContent`。

### 文档

- 补充 Memory、工具来源策略和子 Agent 协作文档。

## [1.0.4] - 2026-04-12

### 重构

- 使用 `metadata.summary` 替代 `displayContent`。

### 文档

- 补充 Memory、工具来源策略和子 Agent 协作文档。

## [1.0.3] - 2026-04-10

### 新功能

- 引入 `ConversationState` 和 `ExecutionEpoch` 作为消息与事务边界。

### 重构

- 改进空值检查和类型安全。

### 文档

- 按 1.0 API 刷新文档。

## [1.0.2] - 2026-04-07

### 测试

- 扩展持久化会话、MCP、子 Agent 和多模态输入的集成测试。

## [1.0.1] - 2026-04-07

### 新功能

- 新增会话恢复并转发 `turn_end` 流事件。

## [1.0.0] - 2026-04-07

### 新功能

- 新增运行时工具目录、基于 patch 的 Skill 激活、上下文溢出恢复、token 预算和后台子 Agent。
- 新增可选文件系统 Memory，并保证确定性顺序。

### 重构

- 移除 ACP，并统一 Agent Loop 和运行时所有权。

## [0.2.8] - 2026-03-31

### 新功能

- 持久化多模态 Session 消息中的图像内容。

## [0.2.7] - 2026-03-29

- 无面向用户的变更。

## [0.2.6] - 2026-03-29

### 重构

- 旧发布脚本支持通过 `.npmrc` 进行 npm 认证。

## [0.2.5] - 2026-03-29

### 修复

- 加固旧版包发布流程。

## [0.2.4] - 2026-03-29

### 构建

- 将仓库从 Bun 迁移到 pnpm 和 Vitest。

### 文档

- 更新 README 和社区链接。

## [0.2.3] - 2026-03-27

### 破坏性变更

- 移除 `BYPASSALL` 权限模式。

### 文档

- 上线 VitePress 文档站。

## [0.2.2] - 2026-03-27

### 修复

- 修复旧发布脚本的 tag 推送。

### 重构

- 替换纯静态工具类。

## [0.2.1] - 2026-03-27

- 无面向用户的变更。

## [0.2.0] - 2026-03-26

### 新功能

- 扩展 Hook 事件与控制流。
- 为 Skills 新增内联命令和运行时副作用。
- 支持 Session 禁用持久化。

### 破坏性变更

- 移除内置 Skill 安装器、版本检查器、默认系统提示和内置 API Key 管理。
- 支持自定义存储根目录。

## [0.1.19] - 2026-03-20

### 修复

- 将所选 registry 传递给 Bun 发布流程。

## [0.1.18] - 2026-03-20

### 新功能

- 支持 Session 禁用持久化。

### 修复

- 稳定并发 Session 的日志路由。

## [0.1.17] - 2026-03-12

### 修复

- 移除文件系统访问检查对 `process.cwd()` 的隐式依赖。

## [0.1.16] - 2026-03-12

### 重构

- 引入 `ContextSnapshot` 管理运行时上下文。

## [0.1.15] - 2026-03-09

### 新功能

- 新增原生 OpenAI 支持并透传自定义 header。

## [0.1.14] - 2026-03-09

### 新功能

- 改进 JSON Schema 到 Zod 的转换。

### 重构

- 统一 Session 运行时所有权，并移除已废弃的插件、命令、Spec 和文件检查点系统。
- 重构 Agent Loop、上下文管理和日志注入。

## [0.1.13] - 2026-02-28

### 新功能

- 导出 `ProviderConfig` 类型。

## [0.1.12] - 2026-02-28

### 构建

- 新增类型声明构建配置。

## [0.1.11] - 2026-02-28

### CI

- 固定使用公共 npm registry。

## [0.1.10] - 2026-02-28

### 新功能

- 新增错误处理辅助函数。

### 重构

- 统一使用 `AgentEvent` 类型。

## [0.1.9] - 2026-02-26

### 重构

- 惰性加载 Provider、拆分 `Agent`，并按实例隔离 MCP registry。

## [0.1.8] - 2026-02-18

### 重构

- 提取 `AgentLoop`、移除旧执行循环并统一事件类型。

### 测试

- 为 Agent Loop、SkillLoader、ToolRegistry 和 ContextCompressor 增加聚焦测试。

## [0.1.7] - 2026-02-12

### 新功能

- 导出模型思维能力检测工具。

## [0.1.6] - 2026-02-12

### 新功能

- 新增进程内 MCP Server。

### 修复

- 成功连接或重连后将 MCP Server 状态设置为 `CONNECTED`。

## [0.1.5] - 2026-02-08

- 无面向用户的变更。

## [0.1.4] - 2026-02-08

### 新功能

- 新增 Session 分叉、Sandbox 检查、MCP resource 和结构化输出。
- 新增文件检查点追踪，该功能已在后续版本移除。

## [0.1.3] - 2026-02-08

### 重构

- 重构 MCP，并移除旧 Copilot 和 Antigravity service。

## [0.1.2] - 2026-02-08

### 重构

- 通过单一流接口统一 Agent 事件。

### 测试

- 新增 Hook、token 计数、路径安全、匹配器、工具创建和输出解析测试。

## [0.1.1] - 2026-02-08

### 新功能

- 新增 `SubagentStart` 和 `TaskCompleted` Hook 事件。

## [0.1.0] - 2026-02-08

- 无面向用户的变更。

## [0.0.1] - 2026-02-08

- 初始发布。
